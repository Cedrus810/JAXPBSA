# JAXPBSA 实测记录

本文件是**所有性能与正确性数字的唯一来源**。`DESIGN.md` 只引用这里，不重复数字。
每条结论都标注了它换卡后还成不成立。

---

## 0. 测量条件（所有数字都适用，除非另行标注）

| | |
|---|---|
| **GPU** | **NVIDIA GeForce RTX 2080 Ti** — Turing sm_75，11 GB，峰值带宽 **616 GB/s**，fp64 = 1/32 fp32 |
| 驱动 | 580.178.04；JAX 默认后端 = `gpu` |
| 精度 | **fp32 数组 + fp64 归约**（标注 fp64 的除外） |
| 体系 | **S4** = PDB 1SPS 链 C+F，1835 原子，96 Å box，padding 20 Å |
| 求解 | tol = 1e-5 相对残差；参考方程走 DST 直解；溶剂方程 MG 预处理的 CG |

> **注意「换卡不变」看的是机制不是单位**：迭代数、G_PB、真残差显然不变；
> 但**由迭代数驱动的时间比也跨卡保持**（warm start 两卡都是 1.07×），
> 而**由固定延迟驱动的比值不会**（MG 在粗网格上 2080 Ti 0.87× vs 5080 0.45×）。
>
> **目标机是 RTX 5090**（32 GB，1792 GB/s，fp64 = 1/64）——**本机不是 5090**。
> 下面 §1 的数字换卡不变，§2–§5 会变。跨机比较请用 `jaxpbsa/benchmark/roofline.py`
> 报**占峰值带宽的百分比**，不要报秒。

---

## 0.1 ⚠️ 收敛判据修正（2026-09-13，此前所有加速比作废并已重测）

原 `pcg_solve` 的停止量是 **`sqrt(rᵀM⁻¹r) / ‖b‖₂`** —— PCG 手里本来就有
`rz = rᵀM⁻¹r`(算 alpha/beta 用)，拿它当停止量是免费的，但它是 **M⁻¹-范数**。
换预处理器(Jacobi → MG)、甚至只把预处理器整体缩放一个常数，都会改变停止时机。
于是**"相同 tol" 在两个求解器下不是相同精度**，任何 MG vs Jacobi、fp32 vs fp64
的加速比都可能只是"精度变松了"。

已改为**真残差 `‖b − Au‖₂ / ‖b·mask‖₂`**，分母固定为源项范数（不用 ‖r₀‖，
否则 warm start 会被惩罚：起点越好要求降得越多）。循环内用递推残差，
**退出前重算一次 `b − Au`** 防 fp32 漂移。代价是每次迭代多一个 `dot(r,r)`。

**重测结果（S4，RTX 2080 Ti，基准 = fp64 Jacobi tol=1e-5）：**

| h | n | 配置 | 迭代 | 真残差 | 时间 | G_PB 误差 | 加速 |
|---|---|---|---|---|---|---|---|
| 1.00 | 97 | fp64 Jacobi | 115 | 9.78e-06 | 65.9 ms | ±0.0000 | 1.00× |
| | | fp64 MG | 18 | 6.62e-06 | 77.6 ms | ±0.0000 | **0.85×（更慢）** |
| | | fp32 Jacobi | 115 | 9.80e-06 | 32.0 ms | +0.0079 | 2.06× |
| | | fp32 MG | 18 | 6.63e-06 | 36.8 ms | +0.0084 | 1.79× |
| 0.75 | 129 | fp64 Jacobi | 143 | 9.55e-06 | 145.3 ms | ±0.0000 | 1.00× |
| | | fp64 MG | 15 | 4.78e-06 | 117.5 ms | ±0.0000 | 1.24× |
| | | fp32 MG | 15 | 4.79e-06 | 56.9 ms | +0.0130 | **2.55×** |
| 0.50 | 193 | fp64 Jacobi | 193 | 9.85e-06 | 768.6 ms | ±0.0000 | 1.00× |
| | | fp64 MG | **11** | 8.34e-06 | 338.9 ms | ±0.0000 | **2.27×** |
| | | fp32 Jacobi | 193 | 9.90e-06 | 259.3 ms | +0.0346 | 2.96× |
| | | **fp32 MG** | **11** | 8.35e-06 | **149.0 ms** | +0.0346 | **5.16×** |

**与旧数字的差异**：
- MG 在 h=0.5 是 **2.27×**，不是我先前报的 1.59×。**不公平的判据反而低估了 MG。**
- MG 的交叉点在 **h ≈ 0.75–1.0**：0.85× → 1.24× → 2.27×，越细越划算。
- MG 迭代数 18 → 15 → **11**（随加密下降）；Jacobi 115 → 143 → 193（上升）。
- fp32 对 G_PB 的偏移 +0.035 kcal/mol（h=0.5），与旧测量一致。

---

> ## ⚠️ 所有性能数字的适用范围
> 本文件里的 ms/帧 全部是 **单个 species 的 PB 求解**（S4 complex 一份），
> **不是**完整的 ΔG_PB = G_C − G_R − G_L（那要三次求解 + 三次编译），
> 更不是完整的 ΔG_MM/PBSA（还缺 MM cross、SA 和汇总）。
> 换算完整 ΔG_PB 每帧吞吐时不能直接用这里的数字。

## 0.15 优化链：从 1800 ms 到 76.8 ms（单 species PB）

全部 **RTX 2080 Ti / S4 1835 原子 / h=0.5**。每一步只改一件事。
「精度中性」= 该步不改变目标精度，加速是纯粹的；否则注明改了什么。

| # | 改动 | 配置 | ms/帧 | 本步 | 累计 | 精度 |
|---|---|---|---|---|---|---|
| 0 | **起点** | fp64，立方 193³，Jacobi 解溶剂 + **PCG 解参考**，tol=1e-7 | **1800** | — | 1.0× | — |
| 1 | DST 直解参考方程 | 同上，参考改 DST | 840 | 2.14× | 2.1× | 中性（精确解） |
| 2 | 真残差判据 + tol 1e-5 | fp64，立方，Jacobi+DST | 768.6 | 1.09× | 2.3× | **判据修正**；tol 放松（两档 G_PB 差 0.002） |
| 3 | MG 预处理器 | fp64，立方，MG+DST | 338.9 | 2.27× | 5.3× | 中性 |
| 4 | fp32 数组（fp64 归约） | fp32，立方，MG+DST | 149.0 | 2.27× | 12.1× | G_PB +0.035（离散误差是 40） |
| 5 | 矩形网格 | fp32，161×161×193 | ~100 | 1.41× | 18.0× | 节点 −30.4%，边界位置变了 |
| 6 | 参考边界分块 | ab=128 | 95.6 | 1.07× | 18.8× | ΔG_PB 差 ≤ 0.00025 |
| 7 | **surface 静态 slice** | 形态学 28.95 → 4.86 ms | **76.8** | 1.24× | **23.4×** | **逐位相同** |

> **第 2 步不是加速。** 它把停止判据从 `sqrt(rᵀM⁻¹r)/‖b‖`（M⁻¹-范数，换预处理器就换判据）
> 改成真残差 `‖b−Au‖/‖b‖`。改之前 MG 和 Jacobi 根本不在同一精度上比，所有加速比无效。
> 修正后 **MG 反而更好**（2.27× 而非原报的 1.59×）——不公平的判据一直在低估它。

**5080 上的对应数字**（用户实测）：第 6 步状态下 h=0.5 fp32+MG = **35.7 ms/帧**，
比 2080 Ti 的 95.6 ms 快 2.68×。迭代数与 G_PB 两台机器完全一致。

---

## 0.16 surface kernel：28.95 → 4.86 ms（6.0×，逐位相同）

形态学占 surface 的 96%，其中 `dilate(ion)`（257 偏移）一项就 58.4%。

| 子步 | 偏移数 | 原 | 现 |
|---|---|---|---|
| rasterize_spheres | 437 | 0.22 ms | 0.22 ms |
| dilate(probe) | 81 | 5.64 ms | **0.65 ms** |
| erode(probe) | 81 | 5.71 ms | **0.78 ms** |
| dilate(ion) | 257 | 17.64 ms | **3.44 ms** |
| harmonic_smooth | 7 | 1.02 ms | 1.02 ms |
| **合计** | | **30.22 ms** | **6.11 ms** |

**真因**：`lax.dynamic_slice` 的起点是 tracer，**XLA 无法融合** —— 257 个偏移就是
257 个独立 kernel，每个完整读一遍 5M 网格再写回。偏移本来就是 host 侧常量，
改成静态 `lax.slice` 后 XLA 把整条 max 链融成一趟访存。

**代价**：编译变慢。测试套件 70 s → 206 s。静态展开把图撑大了。

### 试过但没用 / 不可行的三条（写进代码注释，免得重试）

| 办法 | 结果 |
|---|---|
| pad 提到循环外 | **零收益**。我以为每个偏移都在重新 `jnp.pad` 整个数组，实测 5.64 → 5.61 ms —— XLA 本来就 CSE 掉了 |
| 分组静态展开（chunk 8/16/32/64/128） | **零收益**。静态起点之后 XLA 本来就跨整条 Python 展开链融合，chunk=1 和 chunk=128 都是 0.65 ms。最终代码是朴素循环 |
| 球分解 ball(R) → m×ball(R/m) | **不可行**。连续空间 `ball(a)⊕ball(b)=ball(a+b)`，格点上不成立：h=0.5、R=2.0 拆两次的等效结构元只有 **185 点 vs 目标 257，对称差 72**，会改变表面定义 |

---

## 0.2 当前性能总览（RTX 2080 Ti，S4 1835 原子，h=0.5，真残差 tol=1e-5）

| | ms/帧 | 迭代 | G_PB |
|---|---|---|---|
| 会话起点：fp64 + 立方 193³ + Jacobi 解溶剂 + **PCG 解参考** + tol=1e-7 | **1800** | — | — |
| 现在：fp32 + MG + DST + 矩形 161×161×193 + 静态 slice 形态学 | **76.8** | **11** | −954.0140 |

**23.4×**（完整链见 §0.15）。能量与 fp64 基准差 0.028 kcal/mol，离散化误差量级是 40。

复现：`python scripts/benchmark.py`（子进程隔离、关预分配、编译/稳态分离、真残差判据）。

### 各项贡献（全部在真残差判据下重测）

| 优化 | 端到端 | 备注 |
|---|---|---|
| fp32（+ fp64 归约） | 2.0–2.96× | 误差恒定 +0.035 kcal/mol |
| MG 预处理器 | 2.27× | h=1.0 时 **0.85×（更慢）**，越细越划算 |
| DST 参考解 | 2.14× | 精确解；参考方程本占 69% 迭代 |
| **矩形网格** | **1.41×** | 节点 −30.4%（193³ → 161×161×193） |
| tol 1e-7 → 1e-5 | 1.8× | 两档 G_PB 只差 0.002 kcal/mol |
| 参考边界分块 | **1.07×** | kernel 本身 5.28×，但只占总时间 1.8% |
| MM 参数预置设备 | 2.1×（MM 项） | 2.31 → 1.09 ms |

### 分阶段实测（`scripts/profile_stages.py`，RTX 2080 Ti，h=0.5，fp32）

surface 优化前后对比 —— **Amdahl 在两个方向上都起作用**：

| 阶段 | 优化前 | 占比 | **优化后** | **占比** | 编译 |
|---|---|---|---|---|---|
| 溶剂求解 (MG-PCG) | 62.9 ms | 63.3% | **63.3 ms** | **82.4%** | 14.4 s |
| surface (ε/κ̄² map) | 29.2 ms | **29.4%** | **6.2 ms** | **8.1%** | 1.7 s |
| 参考求解 (DST) | 4.0 ms | 4.0% | 4.0 ms | 5.2% | 0.7 s |
| 参考边界 (库仑和) | 1.8 ms | 1.8% | 1.8 ms | 2.3% | 0.2 s |
| MG 建层 | 1.2 ms | 1.2% | 1.2 ms | 1.6% | 1.2 s |
| charges / 能量 | 0.3 ms | 0.4% | 0.3 ms | 0.4% | 1.0 s |
| **合计** | **99.4 ms** | | **76.8 ms** | | |

端到端单帧 **76.80 ms**（编译 8.48 s），与分阶段之和 76.78 ms 吻合。

**溶剂求解现在占 82.4%**，是唯一还值得动的大块。它已经是 MG-PCG 只跑 11 次迭代，
所以下一步不是换算法，而是**算子本身的访存效率** —— 但这次要按 profile 判断，
不能再用纸面流量反推（§0.16 的教训：我对 surface 的三个猜想里有两个是零收益、
一个不可行，真因是 `dynamic_slice` 阻止融合，光看源码看不出来）。

> **两条撤回**：
> 1. **「surface 只占 4.7%」作废** —— 现在是 **29.4%**。不是当初测错，是 Amdahl：
>    solve 从 710 ms 降到 62.9 ms 后 surface 占比自然上升。**它现在是第二大成本，
>    下一个优化目标应该是它，不是算子融合。**
> 2. **「算子融合还有 2–3×」撤回** —— 那是从"35% 峰值带宽"反推的，而那个数按固定
>    `12×n³×itemsize×iters` 估算：12 次遍历只是 Jacobi-PCG 的估计，不代表 MG 多层，
>    完全没算 FFT、gather 和边界的中间量，默认 itemsize 还是 8（实际已是 fp32）。
>    纸面流量论证不了优化空间，`roofline.py` 已重写为分阶段实测。

### 三条负面结果

**1. warm start 收益很小，且与 MG 此消彼长。**

重测（稳定编译入口、编译与热运行分开、三路对照；RTX 2080 Ti，h=0.5，T=8，步长 0.05 Å）：

| 路径 | 编译 | 热运行 | ms/帧 | 平均迭代 |
|---|---|---|---|---|
| A `lax.scan` + warm | 9.58 s | 563.4 ms | **70.43** | **10.12** |
| B `lax.scan` + cold | 8.75 s | 606.7 ms | 75.83 | 11.12 |
| C Python 循环 + warm（势场留设备端） | 8.44 s | 568.1 ms | 71.01 | — |

- **`lax.scan` 没有额外开销**：A(70.43) ≈ C(71.01)，差 0.8%。
- **warm start：迭代 11.12 → 10.12（−9%），时间 1.08×。** 有收益，但很小。
- 能量一致（cold vs warm 最大差 **1.45e-3 kcal/mol**），两者都 `converged=True`。

> **已在修好的 cold 语义上复测（`scripts/warm_start.py`），且两张卡结果一致**：
>
> | | RTX 2080 Ti | RTX 5080 |
> |---|---|---|
> | A scan+warm | 70.31 ms/帧 | 27.68 ms/帧 |
> | B scan+cold | 75.46 ms/帧 | 29.64 ms/帧 |
> | C Python 循环+warm | 72.04 ms/帧 | 27.46 ms/帧 |
> | 迭代 cold → warm | 11.12 → 10.12 | **11.12 → 10.12** |
> | **warm 时间比** | **1.07×** | **1.07×** |
> | scan 开销 (A vs C) | 1.024× | **1.008×** |
> | 能量差 cold/warm | 2.38e-03 | **2.38e-03** |
>
> **warm start 的增益本身是换卡不变的** —— 它由迭代数降幅（−9%）驱动，每次迭代的成本
> 在分子分母上同步缩放。**这与 MG 不同**：MG 的增益强烈依赖设备（粗网格上
> 2080 Ti 0.87× vs 5080 0.45×），因为它的粗层是 kernel 启动延迟主导。
>
> 所以「时间比」并不天然属于本机专属量 —— **由迭代数驱动的比值会跨卡保持，
> 由固定延迟驱动的比值不会**。分类要看机制，不能只看单位是不是秒。
>
> 复测的必要性：先前 `_scan_cold` 把 carry 当成每帧的 `u_prev`，「cold」实际是
> 「每帧从同一个给定初值出发」。那次恰好传的是默认零势，所以数字没错 ——
> **但那是运气，不是设计**。对照组的定义错了就必须重测，不能靠"应该没影响"。
>
> `lax.scan` 无额外开销这点在两卡上都成立（1.024× / 1.008×），
> 先前报的「慢 7.6×」已确认是重复 tracing 造成的测量 bug。

> **先前报的「scan 比单帧慢 7.6×」和「warm start 零收益」都是我的测量 bug**：
> `solve_trajectory` 每次调用重新定义 `step`，没有稳定的外层 JIT 入口，
> 于是每次调用都重新 tracing/编译 —— 765 ms/帧里混着 9 s 编译时间。
>
> **warm start 与 MG 此消彼长**：Jacobi 要 193 次迭代时省 20% 是省 38 次；
> MG 只要 11 次时省 9% 只是省 1 次。plan §11 把 warm start 当作
> 「轨迹特有、单结构 solver 拿不到」的核心 novelty，**这个定位要按数据下调**。

**2. 参考边界分块端到端只有 1.07%。** kernel 本身 12.88 → 2.44 ms（5.28×）是真的，
但它只占 1.8%。先前报的 1.50× 是假的 —— 我在后台测量运行期间改了 `grid.py`，
前三行跑立方网格、最后一行跑矩形网格。**不要在测量运行期间改源码。**

**3. MG 最粗层轮数完全不影响结果。** coarse_sweeps ∈ {4, 8, 16, 32, 50} 都是
11 次迭代、96–99 ms。「50 轮 Jacobi 在 125 个未知量上浪费调度」的猜想不成立。

---

## 0.3 参考边界分块（精确求和，只改归约顺序）

S4 / h=0.5：**S = 174,082 边界点 × N = 1,835 原子**。边界点方向天然并行，但原本
按原子写成 `fori_loop` 时原子方向是 **1835 轮串行依赖**，每轮只做一次 [S] 向量运算
—— GPU 上是 1835 次小 kernel 的启动延迟，不是算力问题。

| atom_block | kernel 单独 | 端到端 | ΔG_PB 相对 ab=1 |
|---|---|---|---|
| 1 | 12.88 ms | 104.3 ms | ±0 |
| 64 | 3.87 ms | 101.5 ms | +0.000149 |
| **128（默认）** | **3.05 ms** | **97.7 ms** | +0.000045 |
| 256 | 2.44 ms | 100.1 ms | −0.000013 |
| 512 | 2.47 ms | 96.7 ms | +0.000246 |

归约顺序变化对 ΔG_PB 的影响 ≤ 0.00025 kcal/mol，可忽略。默认取 128
（中间量 S×128 ≈ 113 MB，兼顾速度与显存）。**不一次展开 [S, N]** —— fp32 下那是 1.2 GB。

---

## 0.16.5 跨设备验证：RTX 5080 vs RTX 2080 Ti

**换卡不变量完全一致** —— 这是「算法结论只建立在换卡不变量上」这个做法的决定性证据：

| 量 | RTX 2080 Ti | RTX 5080 |
|---|---|---|
| G_PB complex / receptor / ligand | −979.441 / −1244.912 / −619.935 | **逐位相同** |
| ΔG_PB | +885.405 | **+885.405** |
| 迭代 (C/R/L) | 11 / 12 / 10 | **11 / 12 / 10** |
| 真残差 | 8.79e-06 / 4.76e-06 / 7.81e-06 | **完全相同** |

两台机器从同一个 `S4_complex.cif`（sha256 `78d8db8e…`）出发，加载时校验通过。

### 本机专属量：5080 快 2.5–2.6×，但各阶段不均匀

| 阶段 | 2080 Ti | 5080 | 加速 |
|---|---|---|---|
| 溶剂求解 (MG-PCG) | 63.3 ms (82.4%) | **24.43 ms (85.3%)** | 2.59× |
| surface | 6.2 ms (8.1%) | 1.79 ms (6.3%) | **3.46×** |
| 参考求解 (DST) | 4.0 ms | 1.17 ms | **3.42×** |
| 参考边界 | 1.8 ms | 0.59 ms | 3.05× |
| **端到端单帧** | **76.8 ms** | **29.06 ms** | **2.64×** |
| C/R/L 三 species | 222.5 ms | **88.3 ms** | 2.52× |

**surface 和 DST 比求解器更吃带宽提升**（3.4× vs 2.6×），所以求解器占比从 82.4% 进一步升到
**85.3%**。换更快的卡只会让求解器更突出 —— 它是唯一还值得优化的大块。

### MG 的交叉点：两张卡落在同一个节点数区间

完整矩阵（5080，`5080.csv`；2080 Ti 取自 §0.1 重测）：

| h | 节点 | fp64 Jacobi | fp64 MG | fp32 Jacobi | fp32 MG | **fp32 MG 增益** | 2080 Ti 同项 |
|---|---|---|---|---|---|---|---|
| 1.00 | 0.91 M | 117 it / 23.18 ms | 18 it / 27.17 ms | 117 it / 5.59 ms | 18 it / 12.53 ms | **0.45×（慢 2.2 倍）** | 0.87× |
| 0.75 | 2.15 M | 131 it / 67.71 ms | 16 it / 50.19 ms | 131 it / 22.50 ms | 16 it / 16.49 ms | **1.36×** | 1.27× |
| 0.50 | 5.00 M | 193 it / 201.51 ms | 11 it / 90.91 ms | 193 it / 81.54 ms | 11 it / 29.03 ms | **2.81×** | 1.74× |

**交叉点在 0.9–2.1 M 节点之间，两张卡一致**（迭代数 117/131/193 与 18/16/11 也两卡相同）。
但幅度相反：

- **粗网格上卡越快 MG 越吃亏**（0.45× vs 0.87×）—— MG 粗层那几个小网格是
  kernel 启动延迟主导，设备越快，固定延迟的占比越高。
- **细网格上卡越快 MG 越赚**（2.81× vs 1.74×）—— 细层的带宽优势压过粗层的延迟损失。

> **因此 `PBParams.precond` 默认改为 `"auto"`**（`MG_NODE_THRESHOLD = 1.5 M` 节点，
> 取交叉区间的中点）。原先硬编码 `"mg"` 在 h=1.0 上会让程序**慢一倍多**。
> 阈值是两张卡的实测标定，换卡需要复核 —— 但注意交叉点本身（按节点数）在两卡上一致，
> 变的只是两侧的幅度。

### fp32 的收益在 5080 上更大

| h | Jacobi fp64→fp32 | MG fp64→fp32 |
|---|---|---|
| 1.00 | **4.14×** | 2.17× |
| 0.75 | 3.01× | 3.04× |
| 0.50 | 2.47× | **3.13×** |

2080 Ti 上是 2.0–2.96×。5080 的 fp64 是 1/64 fp32（2080 Ti 是 1/32），所以 fp32 更划算。
G_PB 的 fp64↔fp32 偏差仍是恒定量级：h=1.0 差 0.0085、h=0.75 差 0.0176、h=0.5 差 0.0296。

### batch 标度：两张卡都是负收益，但形状不同 —— 机制未定论

| B | 2080 Ti | 5080 | 5080 峰值显存 |
|---|---|---|---|
| 1 | 1.00× | 1.00× | 0.35 GiB |
| 2 | **0.62×** | 0.87× | 0.59 GiB |
| 4 | 0.63× | 0.73× | 1.12 GiB |
| 8 | 0.63× | **0.70×** | 2.47 GiB |

2080 Ti 是**一步跌到 0.62 之后完全平**；5080 是**逐步下滑**。

> **先前「batch 维在最前打散了 stencil 的合并访存」这个解释已撤回。**
> 它预测的是恒定惩罚（平的），符合 2080 Ti 但不符合 5080 的渐降。
> 目前能确定的只有两条：(a) 两张卡上 batching 在生产网格下都是负收益；
> (b) **显存不是原因**（5080 的 B=8 只用 2.47 GiB / 16 GB）。机制待查。

---

## 0.17 ΔG_PB 的误差预算 —— 公共网格的抵消是实测到的

C/R/L 在**同一 origin、同一 nxyz、同一 h** 上求解（DESIGN.md §2）。设计意图是让
G_PB 绝对值里的格点自能离散误差在 C − R − L 中抵消。**实测确实如此，而且程度极强**：

| 加密 | G_C | G_R | G_L | **ΔG_PB** |
|---|---|---|---|---|
| h=1.0 → 0.75 | +256.90 (25.4%) | +246.31 (19.3%) | +44.90 (7.2%) | **−34.30 (3.85%)** |
| h=0.75 → 0.5 | +31.35 (3.2%) | +31.36 (2.5%) | +5.48 (0.88%) | **−5.49 (0.62%)** |

第二行：**G_C 变 31.35，G_R 变 31.36 —— 抵消到 0.01 kcal/mol 以内。**

### 剩余误差几乎全部来自配体（与直觉相反）

```
h=0.75→0.5:   31.35  −  31.36  −  5.48  =  −5.49
              └── 相互抵消 ──┘     └ 没有对手 ┘
```

complex 的误差与 receptor 的误差几乎完全对消（受体 1655 原子主导了 1835 原子的复合物），
**净剩下的是配体那 5.48**。所以要提高 ΔG_PB 精度，**该加密的依据是配体而不是复合物**。

### 误差预算（h=0.5，fp32）

Richardson：两步比值 6.2，余项 ≈ 5.49/(6.2−1)。

| 来源 | 量级 | 占最终 ΔE_MM+ΔG_PB (−61.8) |
|---|---|---|
| **离散化（主导，配体为主）** | **~1.1 kcal/mol** | **1.8%** |
| fp32 数组 | 0.035 | 0.06% |
| 求解器 tol=1e-6 | 可忽略 | — |
| 参考解复用 (u_ref_C = u_ref_R + u_ref_L) | 1.83e-07 相对 | — |

> **不要用绝对值的相对误差去估 ΔG_PB 的误差棒。** G_C 在 h=0.75→0.5 变了 3.2%，
> 按它估会得到 28 kcal/mol 的假误差棒；实际是 1.1 kcal/mol。**差 25 倍。**

### 第一次接近完整的 ΔG_MM/PBSA（S4，规范产物，h=0.5）

```
ΔE_coul (R–L)     -894.80
ΔE_LJ   (R–L)      -52.41
ΔE_MM             -947.21
ΔG_PB             +885.41
──────────────────────────
ΔE_MM + ΔG_PB      -61.81   kcal/mol   (还差 ΔG_SA 和 −TΔS)
```

**静电抵消是对 PB 结果最好的检验**：两个 ~890 量级的数相消只剩 **−9.4（1.1%）**。
PB 若错 5%，抵消后会剩几十 kcal/mol 的偏差，−61.8 会变成完全不同的数。

C/R/L 执行（RTX 2080 Ti，fp32，h=0.5）：

| species | G_PB | 迭代 | 真残差 |
|---|---|---|---|
| complex | −979.441 | 11 | 8.79e-06 |
| receptor | −1244.912 | 12 | 4.76e-06 |
| ligand | −619.935 | 10 | 7.81e-06 |
| **ΔG_PB** | **+885.405** | | 收敛 ✅ |

编译 **20.6 s（三者共用一份）** —— 靠把 R/L 补齐到 complex 的原子数（屏蔽原子挪到盒外、
电荷与半径清零）。不补齐就是三种形状、三次编译。热运行 **222.5 ms/帧 = 74.2 ms/species**。

---

## 0.18 规范产物：转换器与下游的边界

```
scripts/prep_s4.py  ← 转换器: 破损 PDB / PTR / 加氢 / 力场 / 残基命名 全在这里
        │
        ├── data/prepared/S4_complex.cif   拓扑 + 坐标        sha256 78d8db8e…
        ├── data/prepared/S4_system.xml    序列化 openmm.System  sha256 9c3f833f…
        └── data/prepared/S4_meta.json     两个哈希 + 原子索引 + 出处
        │
        ▼
   load_canonical()  ← benchmark / profile / crl / 分析器 全部只读这个, **不再碰力场**
```

序列化 System 把电荷、σ/ε、键、exceptions 全部固化 —— 下游因此**不需要 `openmmforcefields`**
（正是它的 `mamba install` 把 AmberTools、RDKit 等 **50 个包**拖进环境），也消除了力场版本、
模板匹配、OpenMM 把 HID/HIE 归一化回 HIS 这些变数。

### 为什么必须固定产物而不是相信配方

`prep_s4.py` **从来就不是幂等的**。重跑一次 **1835 个原子全部移动**，RMSD **0.78 Å**、
最大 **2.42 Å** —— 足以让同一体系的 G_PB 差 **54 kcal/mol (4.2%)**。
本次会话中段一次为验证组氨酸命名而重跑 prep，就让所有此前记录的绝对能量失去可比性。

三层随机性，逐层剥出：

| 层 | 症状 | 修法 |
|---|---|---|
| `Modeller.addHydrogens` **把氢放在随机位置**（`random.random()`/`random.gauss()`，源码注释原文 "The hydrogens were added at random positions"），再靠内部最小化修好 | 每次结构都不同 | `random.seed(PREP_SEED)` |
| 那次内部最小化建 Context 时**不指定平台**（`modeller.py:1099`），OpenMM 自动挑最快的 = **CUDA**，归约顺序不确定 | 固定种子后仍不幂等 | 显式传 `Reference` 平台 |
| 最终真空最小化走 CPU 多线程 | 力的求和顺序不确定 | `{"Threads": "1"}` |

修完三次运行**逐位相同**。但即便如此，可复现性也只对**当前这个 OpenMM 版本**成立 ——
`addHydrogens` 的实现一变每个原子都会动。**所以固定的是产物，不是配方。**
`load_canonical()` 加载时核对两个 sha256，**漂移会报错而不是被悄悄吸收**。

---

## 1. 换卡不变的量（纯线性代数 / 离散化）

**所有算法决策只建立在本节上。**

### 1.1 迭代数与网格收敛

| h | n | it_solvent (Jacobi) | it_reference (Jacobi) | ref/solv | G_PB (fp64) |
|---|---|---|---|---|---|
| 1.00 | 97 | 142 | 271 | 1.91 | −1239.3 |
| 0.75 | 129 | 174 | 348 | 2.00 | −994.1 |
| 0.50 | 193 | 221 | 494 | **2.24** | −954.0 |

- Jacobi-PCG 迭代数标度实测 **~(L/h)^0.64**。原先估的 (L/h)^1 / 500–1500 次是错的。
- **参考方程占全部迭代的 69%**，且比例随加密还在涨 → 这是 DST 的依据。
  它更难解，因为它没有介电对比、也没有 κ̄² 质量项。
- G_PB 网格收敛：Δ(1.0→0.75) = **+245**，Δ(0.75→0.5) = **+40**。

### 1.2 h ≥ 1.5 不可用

`ball_offsets(1.4 Å, h)` 在 h ≥ 1.5 只返回 **1 个偏移** —— 探针球在格子上退化成一个点，
SES 的闭运算变成恒等映射，**表面定义静默地从 SES 塌回 vdW**。

| h | probe(1.4Å) 偏移数 | ion(2.0Å) |
|---|---|---|
| 2.00 | **1** | 7 |
| 1.50 | **1** | 7 |
| 1.00 | 7 | 33 |
| 0.75 | 27 | 81 |
| 0.50 | 81 | 257 |

数学上正确（2 Å 格距装不下 1.4 Å 的球），但意味着**网格收敛研究只能在 h ≤ 1.0 做**。
我最初用 h=2.0/1.5/1.0 量出的 G_PB 来回摆 600 kcal/mol，就是这个原因，不是求解器错。

### 1.3 MG 单独作为求解器会发散

33³ 网格、随机右端、ε 在球面上跳变，每个 V-cycle 的收敛因子：

| ε 跳变 | 1:2 | 1:10 | **1:80** |
|---|---|---|---|
| 均匀 | 0.21 | — | — |
| 大球 (r=6 Å) | 0.21 | 0.27 | **1.8 → 发散** |
| 小球 (r=2 Å) | 0.21 | 0.21 | **停滞在 3e-2 后上升** |

**ε_in=1 / ε_out=78.5 就是 1:80 那一列**，与溶质大小无关。
均匀系数下 MG 完全正常，所以传输算子本身没问题（`restrict(1)=1`、`prolong(1)=1` 已验）。
原因是标准的：几何插值（与算子无关）无法表示法向导数在介电界面跳 80 倍的解。

**修法**：把 V-cycle 当 **CG 的预处理器**。CG 是极小化，不可能发散；坏预处理器只会变慢。
对称性（CG 前提）成立：restriction = P^T/2³（变分选择）、Jacobi 光滑器对称、nu1 = nu2。
真要 MG 独立求解，需要算子相关插值（Alcouffe–Brandt / de Zeeuw "black box" MG）
或 Galerkin 粗算子，代码量大得多。

### 1.4 MG 预处理后的迭代数（h 无关性）

| h | n | Jacobi-PCG | MG-PCG V(2,2) | 迭代比 |
|---|---|---|---|---|
| 1.00 | 97 | 83 | 15 | 5.5× |
| 0.75 | 129 | 90 | 12 | 7.5× |
| 0.50 | 193 | 107 | **9** | 11.9× |

Jacobi 83→90→107（越细越多），**MG 15→12→9（越细越少）**。这正是多重网格该买到的性质。
Born 离子上同样成立：接 MG 后 h=1.0/0.5/0.25 各只要 **6/7/7** 次迭代（Jacobi 要 50/48/87），能量不变。

### 1.5 T1 Born 离子网格收敛

q=+1, r=2 Å, ε=1/80, κ=0；解析值 **−81.978 kcal/mol**。

| h | G_PB | 误差 |
|---|---|---|
| 1.00 | −87.337 | 6.54% |
| 0.50 | −85.391 | 4.16% |
| 0.25 | −83.335 | 1.65% |

**原先写的「h=0.5 误差 < 3%」没有根据**——锐利介电边界下做不到。
单点阈值也卡不住离散化，测试已改为**收敛性断言**（误差必须随 h 单调下降）。

### 1.6 精度对结果的影响

fp32 相对 fp64 的 G_PB 偏差是**恒定的 +0.035 kcal/mol**（相对 3.7e-5），
而 h=0.75→0.5 的**离散化**变化是 **40 kcal/mol** —— 低三个数量级。

两次求解法是大数相减（u_solvent 与 u_ref 都是自能量级 ~10³ kT/e，差值 ~10¹），
担心 fp32 抵消失效是合理的，但**实测不成立**。

**注意**：不是"全局 fp32"。R–L 交叉能是 NR×NL ≈ 10⁶ 个带符号项强抵消，
纯 fp32 累加丢 ~11 bit，T3 的 1e-6 阈值会挂。**归约必须升 fp64**。

---

## 2. 本机实测 · RTX 2080 Ti — 各阶段耗时

| 项 | h=0.75 (n=129) | h=0.5 (n=193) |
|---|---|---|
| T_surface | 4.2 ms (**3.2%**) | 35.2 ms (**4.7%**) |
| T_solve | 125 ms | 710 ms |
| ms/iter | 0.72 | 3.22 |
| 有效带宽 | 287 GB/s (46.5% 峰值) | 215 GB/s (**34.8%** 峰值) |

**求解器占 95%，surface 只占 4.7%** —— plan §12「surface 是真瓶颈」的预判是错的。
34.8% 峰值说明算子融合还有 **2–3×** 空间，且与 MG 正交。

---

## 3. 本机实测 · RTX 2080 Ti — 精度与速度

h=0.5，同 tol 才可比：

| tol | fp64 | fp32 | fp32 净加速 | G_PB 差 |
|---|---|---|---|---|
| 1e-4 | 0.300 s | 0.114 s | 2.64× | +0.035 |
| 1e-5 | 0.470 s | 0.172 s | **2.73×** | +0.035 |
| 1e-6 | 0.653 s | 0.229 s | 2.85× | +0.035 |
| 1e-7 | 0.841 s | — | | |

- **fp32 本身值 2.7×**。
- 另外 **1.8× 来自 tol 1e-7 → 1e-5**：fp64 下这两档 G_PB 只差 0.002 kcal/mol，
  1e-7 是纯粹的过度求解。默认 tol 已改为 1e-5。
- 合计 fp64@1e-7 → fp32@1e-5 = **4.9×**。

---

## 4. 本机实测 · RTX 2080 Ti — DST vs PCG 参考解

端到端（含表面核）：

| h | PCG ref | DST ref | 加速 |
|---|---|---|---|
| 1.00 | 0.15 s | 0.08 s | 1.78× |
| 0.75 | 0.36 s | 0.16 s | 2.26× |
| 0.50 | 1.80 s | 0.84 s | **2.14×** |

正确性：DST-I 自逆 **1.1e-16**，`poisson_solve` 反演算子 **2.2e-15**，
端到端 G_PB 与 PCG 差 **1e-4 kcal/mol**（相对 1e-7）——精确解，不是近似。

---

## 5. 本机实测 · RTX 2080 Ti — MG 的时间收益（旧判据，已作废）

> ⚠️ **本节数字是在错误的收敛判据下测的，已被 §0.1 取代。** 保留仅为记录。


| h | n | Jacobi-PCG | MG-PCG | 时间比 |
|---|---|---|---|---|
| 1.00 | 97 | 14.4 ms | 21.4 ms | **0.67×（更慢）** |
| 0.75 | 129 | 34.6 ms | 29.5 ms | 1.17× |
| 0.50 | 193 | 114.7 ms | 72.2 ms | **1.59×** |

**时间收益远小于迭代收益（11.9× 迭代 → 只有 1.59× 时间）。**
每个 V-cycle ≈ 8 个 Jacobi 迭代的成本（6 层 × (nu1+nu2) 次光滑 + 传输 + 残差），
11.9 / 8 ≈ 1.5，与实测吻合。**原先估的 ~7× 是错的。**

趋势 0.67 → 1.17 → 1.59：**越往细网格走 MG 越划算**。

光滑次数扫描（h=0.5）：

| | 迭代 | 时间 | 加速 |
|---|---|---|---|
| V(1,1) | 14 | 79.3 ms | 1.45× |
| V(2,2) | 9 | 73.2 ms | 1.57× |
| **V(3,3)** | **7** | **68.5 ms** | **1.68×** |

多光滑反而更好：省下的一次 CG 迭代（含整个 V-cycle + 算子 + 两个内积）比多两次光滑贵。

---

## 6. 本机实测 · RTX 2080 Ti — batch 标度

每个 (h, B) 跑在**独立进程**里（见 §8 第 11 条：同进程顺序跑会让编译产物和显存累积，
先前那组数据已作废），并关闭 `XLA_PYTHON_CLIENT_PREALLOCATE`，同时记录峰值显存。

**h = 1.0（n=97，单帧数组 4 MB）**

| B | 总时间 | ms/frame | frame/s | 峰值显存 | 相对 B=1 |
|---|---|---|---|---|---|
| 1 | 54.9 ms | 54.89 | 18.22 | 0.09 GB | 1.00× |
| **2** | **42.3 ms** | **21.13** | **47.32** | 0.13 GB | **2.60×** |
| 4 | 193.4 ms | 48.36 | 20.68 | 0.24 GB | 1.14× |
| 8 | 574.2 ms | 71.78 | 13.93 | 0.44 GB | 0.76× |

**h = 0.5（n=193，单帧数组 29 MB）—— 生产网格**

| B | 总时间 | ms/frame | frame/s | 峰值显存 | 相对 B=1 |
|---|---|---|---|---|---|
| 1 | 110.6 ms | 110.65 | 9.04 | 0.64 GB | 1.00× |
| 2 | 355.0 ms | 177.48 | 5.63 | 1.04 GB | **0.62×** |
| 4 | 702.0 ms | 175.50 | 5.70 | 1.56 GB | 0.63× |
| 8 | 1411.5 ms | 176.43 | 5.67 | 2.78 GB | 0.63× |

### 结论

1. **绝对不是显存。** 全程最高 2.78 GB / 11 GB。先前"B=4 断崖 = 显存压力"的说法
   是测量工具污染造成的，已作废。
2. **生产网格 (h=0.5) 上 batching 是恒定 0.63× 的惩罚**，B=2 起就出现，之后完全平。
   总时间随 B **精确线性**增长，只是每帧成本一次性变差 1.6×——这不是逐渐饱和，
   而是**批处理版 kernel 本身效率更低**。最可能的原因是 batch 维加在最前，
   打散了 stencil 的合并访存。可验证的改法：把 batch 维放到末轴。（未测）
3. **粗网格 (h=1.0) 上 B=2 有 2.60×，但窗口很窄**，B=4 就掉到 1.14×，B=8 变负收益。
   B=1→2 总时间反而从 54.9 ms 降到 42.3 ms（两倍的活、更短的时间），
   说明 B=1 时 MG 的几层小粗网格是**kernel 启动延迟主导**，batching 把它摊掉了。

**所以 batching 的适用范围是：粗网格 + 小 B（≈2），用来摊掉 MG 粗层的启动延迟。**
在主 benchmark 工况（h=0.5）上它只会让吞吐降到 0.63×。

---

## 7. 加速来源排序（按实测）

全部在**真残差判据**下重测（§0.1），h=0.5：

| 来源 | 收益 | 状态 |
|---|---|---|
| **MG 预处理器** | **2.27×** @ h=0.5（h=1.0 是 0.85×，越细越好） | 完成 |
| DST 直解参考方程 | **2.14×** | 完成 |
| fp32（+ fp64 归约） | **2.0–2.96×** | 完成 |
| tol 1e-7 → 1e-5 | **1.8×** | 完成 |
| MM 参数预置设备 | **2.1×**（MM 项，2.31 → 1.09 ms） | 完成 |
| DST 改 rfft | 与 fft 逐元素相同(差 0.0)，速度 A/B 待测 | 完成 |
| **fp32 + MG 合计** | **5.16×** @ h=0.5 | 完成 |
| 算子融合（35% 峰值 → ?） | 估 2–3×，**未做** | 下一个最大单项 |
| batching | **h=0.5 下 0.63×（负收益）**；h=1.0 下 B=2 有 2.60× 但窗口极窄 | 已测 |

**「Batch first」这个口号站不住**：batching 不在加速来源里。它的价值在粗网格/小体系
（单帧喂不饱 GPU 时）和摊销编译开销，不在主 benchmark 工况。

---

## 8. 已抓到的 bug 与测量陷阱

本节存在的理由：这些**全部是静默失败**——不报错，只给一个看起来合理的错数字。

| # | 问题 | 症状 | 怎么抓到的 |
|---|---|---|---|
| 1 | **PB 源项漏了 560.75 因子** | G_PB 小 560 倍 | T1 Born 解析值 |
| 2 | **κ̄² 与 κ² 混用** | 体相屏蔽长度差 √78.5 ≈ 8.9 倍 | T2 Debye 拟合 |
| 3 | **Dirichlet 边界写进算子** | CG 收到仿射算子，前提破产，给出 −512 而非 −82 | T1 Born 解析值 |
| 4 | **雅可比对角元在 8 个角点为 0** | `0/0 = NaN` → `NaN*0 = NaN` 污染内积 → `cond` 恒 False → **求解器一次迭代都不跑，静默返回 u0**。单帧路径靠 XLA 把 `x*0` 折成 `0` 侥幸没暴露，vmap 换了融合方式就崩 | `test_batch_invariance` |
| 5 | **`jaxpbsa/__init__.py` 强制开 x64** | 覆盖调用方设置，**两轮 fp32 测量全是 fp64 跑的** | 打印 dtype |
| 6 | **numpy 标量静默提升 dtype** | `grid.origin` 存的是 np.float64（强类型），把整条 fp32 路径提回 fp64 | `test_dtype_is_not_silently_promoted` |
| 7 | **不带 dtype 的数组创建** | `jnp.zeros(shape)` / `jnp.where(m,1.,80.)` / `fori_loop` 初值在 x64 下默认 float64 | 同上 |
| 8 | **`TER` 写在残基循环里** | 每个残基被当成独立链，肽键全断 | 代码复查 |
| 9 | **NeRF 第一项符号错** | 键角做成 180°−angle | `_measure`/`_nerf` 往返自检 |
| 10 | **h 选在 ≥1.5** | G_PB 来回摆 600 kcal/mol，看起来像求解器发散 | 查 `ball_offsets` 返回值 |
| 11 | **测量工具在同进程顺序跑多个 B** | 编译产物与显存单调累积 → h=0.75/B=1（9 MB 数组、11 GB 卡）也 OOM。**先前那组 batch 断崖数据因此作废**；独立进程重测后结论完全不同（真因是 kernel 效率，不是显存，见 §6） | 物理上不可能的 OOM |
| 12 | **并发跑两个 GPU 进程** | JAX 默认预分配 75% 显存，第二个进程伪 OOM，甚至触发 `bfc_allocator` 崩溃 | B=1、4 MB 数组也 OOM |

| 13 | **停止判据用了预处理器范数** | `sqrt(rᵀM⁻¹r)/‖b‖` 而非 `‖r‖/‖b‖`。换预处理器就换了判据 → **"相同 tol" 不是相同精度**，所有加速比失去意义 | code review |
| 14 | **`pcg_solve` 位置参数错位** | 加了 `precond` 形参后，`params.tol` 位置传进了 `precond`、`max_iter` 进了 `tol`。切回 PCG 对照必崩 | code review |
| 15 | **`.astype(fp64).sum()` 物化整个副本** | [B,NR,NL] 百万元素先转成 float64 数组再求和，凭空多一趟访存 → **fp32 比 fp64 还慢**。应写 `sum(dtype=)` | fp32 反而更慢 |
| 16 | **静态参数每帧重传设备** | `jnp.asarray(params.charge, float32)` 在每次调用里做 host→device 转换，占 MM 总时间的一半 | fp32 仍比 fp64 慢 |
| 17 | **MM 精度策略形同虚设** | `np.empty(n)` 默认 float64 + `jnp.asarray` 不带 dtype → 逐对乘积全程 float64，坐标是不是 fp32 完全不影响 | code review |

**教训**：每条性能结论在写进文档前，先问"这个测量工具本身对不对"，
再问"两边真的在同一个精度上吗"。第 13 条让**全部**加速比作废过一次。
第 5、11 两条都是**测量工具的 bug 伪装成物理结论**。

---

## 8.1 review 九条的落实情况

| # | 内容 | 结果 |
|---|---|---|
| 1 | CG 停止条件用了预处理器范数 | **属实**。已改真残差；`params.tol` 位置错位也修了。**重测后 MG 比原报的更好**（2.27× vs 1.59×）——不公平判据反而低估了它 |
| 2 | 参考边界串行累加 | **属实**。分块后 kernel 5.28×，但端到端只 1.07%（它占 1.8%） |
| 3 | DST 算了用不到的半频谱 | **属实**。改 rfft，与 fft **逐元素差 0.0** |
| 4 | MG 粗层固定 50 轮 | **不成立**。4 vs 50 完全没差别 |
| 5 | MM 精度策略没落实 | **属实，且比表面更深**。还挖出 `.astype().sum()` 物化副本、静态参数每帧重传（占一半时间） |
| 6 | 网格可少算 30% 节点 | **属实**。161×161×193，端到端 1.41×。multigrid 只看末轴的 bug 也修了 |
| 7 | 每帧冷启动 + 默认返回整张势场 | **属实**。势场改可选（默认关）；warm start 已实现但**当前无墙钟收益**，且 scan 路径本身慢 7.6× |
| 8 | 参考场可 3 次降 2 次 | **属实**。已文档化数学关系，待 C/R/L 汇总接口接入 |
| 9 | 「2–3×」没有依据 | **属实**。已撤回并重写为分阶段实测 |

---

## 9. 下一步（按价值排序）

### 1. ΔG_SA —— **已完成**（见 §11）

JAX Shrake–Rupley，`sa/jax_sr.py`。对暴力法 rel < 1e-6、对 zsasa 参照物总量 4.6e-5、
对 Amber LCPO 0.3%（§12.1）。三 species 131 → 14.4 ms/帧，占每帧从 37% 降到 8%。

> 当初写的「复用 surface.py 的 cell-list gather」**没有采用**：surface.py 是
> grid/scatter 的，映射是 atom→node 不是 atom→atom，没有可复用的原子邻居表。
> 实际走 dense N² + `top_k`，显存靠逐原子分块压平（§11.3）。
>
> 验收基准也换了：**没用 mdtraj**（它不接受逐原子自定义半径，表达不了 mbondi2
> 对氢的成键依赖），改用 zsasa（可喂同一套半径）+ 暴力法 + Amber LCPO。

### 2. 外部对拍 T6 —— **已完成**（见 §12），但结论比预期复杂

`scripts/validate_mmpbsa.py`，走 AmberTools **MMPBSA.py**（整条链，不只 PB）。

当初判断「`pbsa` 比 APBS 更合适」是对的（半径本就是 mbondi2、力场已是 Amber 体系），
但**理由要改**：真正的原因不是省对齐负担，而是 **APBS 与我们同方法族**
（同方程、同类离散、都是格点 FD），对上了只证明实现了同一套离散化。
MMPBSA.py 是另一套实现栈，而且验的是**整条 ΔG_MM/PBSA**。

> 当初记的阻碍「`tleap` 报 N 端 ALA 的 H / HIE 的 HD1 does not have a type」
> **绕过去了**：不走 tleap，用 `parmed.openmm.load_topology` 从规范 System XML
> 直接出 prmtop（charge/epsilon 逐位相同，sigma 4e-16）。少一条依赖，也少一次
> 力场重建的机会。唯一要处理的是 907 个被 HBonds 约束的 X–H 键没有
> HarmonicBondForce 项——填 k=0（single-trajectory 下键项逐项抵消），不编力常数。

**判据必须跟着参照物走**（§12.2）：ΔE_MM 是硬断言（实测 5.3e-6 / 1.8e-4），
ΔG_PB / ΔG_SA 是方法比较。原定的「ΔG_PB < 2%」只对 **APBS** 有效——
对 MMPBSA.py 实测 5.0%，且**两边各自都已网格收敛**，不是精度问题。
APBS 那条逐参数归因**仍然要做**，它是唯一能把这 5% 拆开的工具。

### 3. M0 轨迹 —— benchmark 的输入

结构已规范化（`S4_complex.cif`，sha256 已固定），但**生产 MD 未跑**：
溶剂化加盐 → 最小化 → NVT/NPT 平衡 → ≥10 ns 生产 → strip 水和离子 → unwrap → DCD。

没有它，M9 的 benchmark 矩阵（10–10,000 帧）就没有真实输入，warm start 的收益也只能
用随机游走模拟。这一步的耗时和踩坑量不低于一个求解器里程碑 —— **它原本完全不在计划里**。

### 4. 非对称网格 —— **又快又准，最高优先**（§15.7–15.8）

C/R 共用 h=0.75 大盒、配体用紧盒 h=0.25：**162.0 ms/帧 vs 现状 197.1（快 18%）**，
同时 ΔG_PB 885.4 → 851.7，**与 Amber 的差距 5.0% → 1.0%**。
不是取舍 —— 现在的代码把算力花在会抵消的 C/R 上（§15.1），而配体欠解。

**做之前必须清掉的**：S1–S3 复核；紧盒把 origin 对齐与 padding 混在一起要拆；
`G_L|h=0.25` 本身也未收敛。三次编译（现在一次）在热进程里占 10k 帧轨迹的 1.3%，可接受。

### 5. 分数体积 ε —— 打摆放噪声

§15.4 的 **8.39 kcal/mol** 是纯离散化噪声，比全局加密的效果（5.49）还大，
而 `swin` 平滑被证伪（压不住）。face 被溶质占多少而非二值，**保持 7 点 SPD 算子、
求解器不动**。不要做 IIM（跳跃条件进 stencil 可能破坏 SPD，而 MG 在 1:80 下当 solver
本就发散，§1.3）。

### 6. 曲面解析基准（Kirkwood 球 / 双球）

现在只有 Born（单球、均匀 ε），测不出曲界面的 voxel 化误差。要主张 #5 更准就必须有它。

### 7. 继续找可融合的链

已兑现两次同一机制（`dilate` 6.0×、`_smooth` GPU 工作量 −20%）。
`v_cycle` 的限制/延拓、`pcg_solve` 的向量操作还是独立 kernel。
判据同一条：**trip count / 结构是不是编译期已知**。

### 8. 多中心屏蔽库仑边界 —— **降级**

原本排第 1，理由之一是「能让 padding 变小」。**§15.8 证伪**：单中心 DH 在当前 padding
下已收敛（20→40 Å 只动 0.003）。剩下的理由只有「它是 pbsa 差异里点名过的来源」，
而特化已把差距收到 1.0%（8.8 kcal/mol），空间不大。仍然便宜（~3 ms/帧），
零结果也能把残余归到能量泛函。

### 9. NLPB（Newton–Krylov）—— 独立立题

`J = −∇·(ε∇·) + κ²cosh(φ)·` 仍 SPD，现有 PCG+MG 只需改对角。
S4 的 −4 磷酸肽是正当测试对象。但**是模型变化**，不能再用同一批参照物验证。

## 10. 明确不做 / 已证伪的方向

| 方向 | 结论 |
|---|---|
| batching | 两卡实测均为**负收益**（h=0.5 下 0.63× / 0.70×），显存不是原因，机制未定论 |
| warm start 作为核心 novelty | **1.07×** —— 与 MG 此消彼长，MG 把迭代压到 11 后只能再省 1 次。<br>已在**修好的 cold 语义**上复测（迭代 11.12→10.12，−9%），结论不变 |
| MG 最粗层轮数调优 | **只在 min_n=7（终层 252 点）下成立**：4 与 50 无差别。<br>终层一变大就完全不同 —— 41×41×49 上 sweeps=4 让迭代数从 29 涨到 **69**（§14.4）。<br>**这条旧结论不能外推**，`coarse_sweeps` 必须跟 `mg_min_n` 一起调 |
| 球分解 `ball(R) → m×ball(R/m)` | **不可行**，格点上 `ball(a)⊕ball(b) ≠ ball(a+b)` |
| 形态学分组展开（chunk） | **零收益**，静态起点后 XLA 本就跨整条链融合 |
| 算子融合的「2–3×」 | 依据（纸面流量）已撤回，需按 profile 重新判断 |
| 用 GB 做 PB 的验证基准 | **不成立**，GB 是 PB 的近似 |
| XLA CUDA-Graph 开关（`--xla_gpu_enable_command_buffer=…,WHILE`）| **更慢**（240.7 / 233.0 vs 221.8）。不消除循环边界，只加 graph 实例化成本（§14.5）|
| 两进程并发测 GPU 饱和度 | **方法无效**：无 MPS 时同卡两 context 由驱动时分复用，1.00× 既可能是饱和也可能只是串行化 |
| 挂 OpenMM plugin / 手写 CUDA 来「消掉调度开销」| **前提不成立**：GPU 已 96% 满，没有调度开销可消（§14.2）。手写 kernel 只在「每趟搬更少字节」上能赢，那是融合的事 |
| `jax.export` → StableHLO → PJRT C++（做成 JaxForce 那样）| **不做**。速度收益为零（同一 XLA/kernel/PJRT，且 GPU 已 96% 满）。唯一真收益是编译延迟，而 84% 由一行持久编译缓存拿到（36.5 → 5.9 s，§15.6）。<br>**结构上也不成立**：`build_levels` 是 host 侧按具体 shape 的 Python 循环、`ball_offsets` 的偏移个数由 radius/h 定，**没法符号化** → 每个 grid shape 一份产物，而 shape 随体系和 h 变。<br>JaxForce 成立是因为**形状固定 + 调用 10⁶ 次**，我们是**形状可变 + 10⁴ 次**，两个前提都反 |
| 拿 `nvidia-smi` 的 `utilization.gpu/memory` 判占用 | 它们是「采样期内有活动的时间占比」，不是占用率也不是带宽占比。可用于**否证**（读数低 = 真有问题），不能用于**确证** |

---

# 11. SA 项：JAX Shrake–Rupley（`sa/jax_sr.py`）

zsasa 那个外部 CLI **不是后端也不是依赖**——它只做性能/结果对拍，已挪到
`tests/zsasa_ref.py`，与 APBS 同一待遇（外部进程调用，不进包）。SA 现在只有一个实现。

## 11.1 正确性

| 对拍对象 | 结果 |
|---|---|
| 解析解：孤立球 4π(r+p)² | rel < 1e-6（无邻居时判据退化，是**恒等**而非近似）|
| 解析解：两球重叠球冠、远离可加 | 过（P=4000）|
| **暴力法**（显式建 pᵢₖ 再逐个测距） | **rel < 1e-6** |
| zsasa 参照物，S4 总 SASA，P=960 / 4000 | rel **4.6e-5 / 5.8e-5** |
| zsasa 参照物，逐原子 | 中位 rel **7e-3** —— 预期对不上，两边点集不同 |

暴力法那条是**唯一**能验「判据代数化」的测试。`jax_sr` 从不构造采样点：

```
|p_ik − x_j|² = d_ij² + R_i² + 2 R_i · u_k·(x_i − x_j)
埋住 ⟺  2 R_i · u_k·(x_i − x_j)  <  R_j² − R_i² − d_ij²
        └── u[P,3] @ disp[K,3]ᵀ 一次矩阵乘 ──┘
```

解析球测试发现不了这步写错——无邻居时两边都退化。**判据是逐点布尔的，
一个点判反就是 4πR²/P ≈ 2e-4 的相对差；卡 1e-6 比它小 200 倍 = 逐点判定完全相同。**

## 11.2 K 近邻的硬闸

判据 `K ≥ maxᵢ |{j : d_ij < R_i + R_max}|`。**不能收紧成 `|{j : d_ij < R_i + R_j}|`**
（S4 上 116 vs 136，看着省 15%）：top_k 按**距离**排序，半径不进排序，
近处的小原子（不遮挡）会挤掉远处的大原子（真遮挡）。

**自动探测两条路都实测过，都不安全**：

| 来源 | 所需 k |
|---|---|
| CIF（真空最小化的制备态）| 131 —— 不是 MD 系综构象，**欠 5** |
| MD 帧 0 | 126 —— **欠 10** |
| MD 40 帧（10 ns）跨度 | **123 – 136** |

单帧探不出系综上界；且 k 是 static argnum，逐块重探会反复触发重编译。
默认 **192**（40% 余量，且这是堆积密度限的量：6.4 Å 球内 ~0.1 原子/Å³ ≈ 110，
换个折叠蛋白量级不变）。闸直接报出该填多少，一次重试必中。

**闸是充分条件，会早报**（S4 单帧，k=256 为基准）：

| k | 128 | 96 | 80 | 64 | 48 | 32 |
|---|---|---|---|---|---|---|
| rel err | 0 | 0 | 5e-5 | 1.2e-4 | 1.4e-3 | **1.4e-2** |

闸在 k<134 就拦，面积到 k=80 才真动——保守 1.7×，是该错的方向。
误差**恒为正且单调**（丢遮挡 ⇒ 暴露变多），所以没有闸就是 §8 那类
「不报错、只给看起来合理的错数字」。

## 11.3 显存的 O(N²) 墙（已拆）

邻居搜索原先先物化整张 `[N,N]` 的 d2。而**时间对 N 几乎线性**
（主成本是逐原子那次 `[P,K]` 矩阵乘）——所以这堵墙是白挨的：

| N | d2 矩阵（旧）| 旧 ms/帧 |
|---|---|---|
| 1835 | 13 MB | 10.3 |
| 8000 | 244 MB | 54.5 |
| 16000 | 977 MB | 138.8 |
| 32000 | 3906 MB | **OOM**（11 GB）|

挪进已有的逐原子分块后峰值是 `B × chunk × (P·K + N)`，**N 的二次项消失**，
S4 结果逐位不变（6342.998 Ų），代码还少一层。

`chunk` 默认按 **B 缩放**：分块的收益只在 B 小时才值得拿显存换。
实测 S4，B=1 时 chunk 42→256 是 **14.4→10.0 ms（−31%）**，B=16 时 6.96→6.74
几乎没差（批维已把 GPU 喂饱）。不除 B 就会在 B=16 上白占 5.8 GB。

## 11.4 速度（RTX 2080 Ti，S4 1835 原子，P=960）

| | zsasa 参照物 (host) | jax_sr B=1 | B=4 | B=16 |
|---|---|---|---|---|
| 单 species | 41 ms/帧 | 11.6 | 8.3 | **7.2** |
| 三 species（ΔG_SA）| 131 ms/帧 | 25.0 | 16.7 | **14.4** |
| 峰值显存 | — | 553 MB | 554 | 554（**与 N 无关**）|

SA 从「每帧多 59%」降到**占每帧 8%**。

---

# 12. 端到端外部对拍：AmberTools MMPBSA.py

`scripts/validate_mmpbsa.py`。**这不是 APBS 的替代，两者分工不同**：

| 参照物 | 验什么 | 判据 |
|---|---|---|
| 解析解（Born / Debye，`test_pb.py`）| PB 求解器本身 | **硬断言** |
| **MMPBSA.py** | **端到端 ΔG_MM/PBSA**；ΔE_MM 的正确性 | ΔE_MM 硬断言；PB/SA 只记录 |
| APBS | ΔG_PB 逐参数归因（§3.12）| 同方法族，可卡 2% |

**为什么需要它。** APBS 是数值 FD+多重网格求解器（**不是解析解**），与我们
**同一个方法族**：同一个方程、同一类离散、都是格点 FD。对上了只证明「两边实现了
同一套离散化」。MMPBSA.py 是**另一套实现栈**——Amber `pbsa`（不同离散/表面/能量泛函）
+ LCPO（不是 Shrake–Rupley）+ sander 的 MM——而且验的是整条链，不是三项里的一项。

## 12.1 结果（S4 canonical 帧，h=0.5，参数逐项锁死）

| 项 | MMPBSA.py | jaxpbsa | Δ | rel | 性质 |
|---|---|---|---|---|---|
| ΔE_coul | −894.799 | −894.803 | −0.005 | **5.3e-06** | 正确性断言 |
| ΔE_LJ | −52.398 | −52.408 | −0.010 | **1.8e-04** | 正确性断言 |
| ΔG_PB | 842.882 | 885.405 | +42.523 | 5.0e-02 | 方法比较 |
| ΔG_SA | −5.746 | −5.730 | +0.017 | **2.9e-03** | 方法比较 |
| ΔSASA | −1149.24 Ų（由 γ 反推）| −1145.91 Ų | | 2.9e-03 | |

**ΔG_SA 那 0.3% 是这轮最强的一条**：LCPO（解析近似）和 Shrake–Rupley（采样）
是两个毫不相干的算法，对到 0.3%。

## 12.2 ΔG_PB 那 5% 不是 bug，也不会收敛掉

**两边各自都已网格收敛**：

| | h 扫描 | ΔG_PB 变化 |
|---|---|---|
| pbsa | 0.5 → 0.25（842.88 / 843.37 / 843.82 / 843.92）| **1.04（0.12%）** |
| 我们 | 0.75 → 0.5 | 5.49（0.62%，§0.17）|

差的是**离散化栈本身**。从 pbsa 真正吃到的 mdin 逐项可见：

| | pbsa | 我们 |
|---|---|---|
| `bcopt=5` 边界 | 全部格点电荷求和（多中心）| **单中心** Debye–Hückel |
| `eneopt=2` 能量 | 介电边界**面电荷** | 反应场 ½Σq(u_solv−u_ref)，两次求解 |
| `nfocus=2, fscale=8` | 两层聚焦，粗层 h=4 Å | **单层** |
| `smoothopt=1` | 按边界占比加权调和平均 | 面上调和平均 + swin=0.5 球邻域 |
| `accept=1e-3` | tol 1e-3 | **1e-5** |

**换求解器不改答案**（ICCG / 多重网格 / CG 收敛到同一个解），所以「用同一个求解器」
这条路不通——而且 MMPBSA.py 14.0 **根本不透传** `ipb` / `nfocus` / `solvopt`：
namelist 解析器收下，然后静默丢弃，从不写进 pbsa 的 mdin。脚本已加落地校验。

**不要拿 §T6 的「ΔG_PB < 2%」卡这里。** 那条是给 APBS 写的（同方法族才能逐参数
对齐到 2%）。判据必须跟着参照物走。

## 12.3 抓到的两个坑

1. **`istrng` 单位是 M，不是 mM。** MMPBSA.py 内部再 ×1000 写给 pbsa。传 `150.0`
   会变成 **150 M**（高 1000 倍）且不报错。修正后 ΔG_PB 842.598 → 842.882——
   **只动 0.28**，因为盐效应在 C−R−L 差分里基本抵消。bug 是真的，对这个量影响不大。
2. **MMPBSA.py 14.0 的默认值和我们不是一套**：`exdi` 默认 80.0（我们 78.5）、
   `istrng` 默认 0.0、**`inp` 默认 2**（γ=0.0378, β=−0.5692，我们是 INP=1 的
   0.005/0.0）。这是 §3.10「论文必须写明用的是哪一档」那条的实证——
   **每一项都必须显式写，不能吃默认值**。

---

# 13. 完整 ΔG_MM/PBSA 的性能：延迟 vs 吞吐

**这两个指标必须分开报，混起来会得出相反的结论。**

机器：Intel 双路，**40 物理核 / 80 线程，2 个 NUMA 节点** + 1× RTX 2080 Ti。
体系 S4（1835 原子），h=0.5，fp32，MD 轨迹帧。

## 13.1 延迟（在线：一帧一帧来，CPU 还在跑产生它们的 MD）

| | 单帧墙钟 |
|---|---|
| MMPBSA.py（1 帧 = 1 进程）| **5806 ms** |
| **jaxpbsa（1× 2080 Ti）** | **325 ms**（中位，范围 311–333）|
| | **17.9×** |

## 13.2 吞吐（离线：帧管够，整台机器随便用）

| 配置 | 硬件 | ms/帧 |
|---|---|---|
| MMPBSA.py 串行 | 1 核 | 5806 |
| MMPBSA.py 8 路并帧 | 8 核 | 1158 |
| MMPBSA.py 32 路 | 32 核 | 340 |
| MMPBSA.py 40 路（逐核 `--physcpubind` + `--membind`）| **满机 40 物理核** | **292** |
| jaxpbsa | **1× RTX 2080 Ti** | 320 |

**满机吞吐上 Amber 略快（292 vs 320）。** 不要引用「26×」那种数——那是拿它的
单核比的，不成立。

> MMPBSA.py.MPI 需要 mpi4py（本机未装）。上表用 N 个并发串行进程等价模拟，
> 这正是它 MPI 在做的事（按帧切分），且省掉 MPI 开销，**对 Amber 更有利**。
> 64 路是超线程（物理核只有 40），不是有效标度点，已排除。

## 13.3 怎么读这两张表

- **Amber 赢在「帧之间」，我们赢在「帧之内」。** 它 embarrassingly parallel：
  40 帧丢给 40 核，**每帧仍然是 5.8 s**。在线场景两个前提都不成立——帧一帧一帧来，
  而且 CPU 正忙着跑 MD。
- **这是一张 2018 年的卡对一台双路 40 核服务器。** §3 跨设备实测 PB：
  2080 Ti 222.5 ms/帧 vs 5080 **88.4**（2.5×）。按同比例 5080 上约 **130 ms/帧**，
  是满机的 2.2×。
- **批处理那一档还没吃。** PB 现在是逐帧 `triplet`（B=1）。§2 算过 h=0.5/fp32
  在 11 GB 上能到 B≈5，32 GB 卡更多。

## 13.4 每帧成本构成（2080 Ti，canonical 帧）

| 项 | ms/帧 | 占比 |
|---|---|---|
| ΔE_MM | 0.64 | 0.3% |
| **ΔG_PB (C/R/L)** | **222.01** | **91.5%** |
| ΔG_SA (C/R/L) | 20.02 | 8.3% |
| 合计 | 242.67 | = 4.1 帧/秒 |

SA 换 JAX 之前是 131 ms/帧，总量 353.6 → **242.7（1.46×）**，
SA 的占比从 37% 降到 8%。**PB 是唯一还值得继续优化的项。**

---

# 14. PB 求解器的归因：一次几乎全错的诊断，和最后那三行

**结论先放前面**：GPU 一直是 **96% 满**的；瓶颈是**搬的字节数**，不是调度、不是 kernel
启动、不是粗层。把 `_smooth` 的 `fori_loop` 换成 Python 展开（`sweeps` 是 host 侧常量），
XLA 融掉整条链，**GPU 工作量 219.4 → 176.4 ms/triplet，操作数 11,987 → 3,610**，
数值逐位不变。

## 14.1 测量陷阱：`nsys` 默认不展开 CUDA Graph

这一节大部分篇幅是**记录一个错误诊断**，因为它比结论更容易重犯。

`nsys profile -t cuda` 默认 `--cuda-graph-trace=graph`：**graph 里的 kernel 只记一次
graph 启动，不展开成节点**。于是 kernel 时间被严重低估，而被低估的那部分在时间轴上
表现为"空隙"。据此我得出过下面这串结论，**全部是采集假象**：

| 作废的结论 | 真实情况 |
|---|---|
| ~~GPU 忙 38%、闲 62%~~ | **忙 96%** |
| ~~每个 GPU 操作 5.6 µs 调度成本~~ | 中位空隙 0.5 µs，调度不是瓶颈 |
| ~~空闲集中在 161 个 ms 级的洞里~~ | 那些"洞"里是没被采到的 graph 节点 |
| ~~MG 有 2.6× 空转~~ | 有约 20% 可回收，但机制完全不同 |

**更该记住的是判断顺序错了**：`nvidia-smi` 报 SM 99%，我用一个错误的 trace 去推翻它。
两个信号打架时，先怀疑新工具的采集口径，而不是先怀疑旧信号。

修法：`nsys profile -t cuda --cuda-graph-trace=node`。**改前改后必须用同一种口径重测**
——否则"优化前 vs 优化后"比的是两种采集方式。

## 14.2 真正的归因（`--cuda-graph-trace=node`，同一张卡、canonical 帧、h=0.5/fp32/B=1）

| | 墙钟 ms/triplet | GPU 忙 | 忙% | GPU 操作/triplet |
|---|---|---|---|---|
| `fori_loop`，min_n=7 | 228.3 | 219.4 | 96% | 11,987 |
| **展开，min_n=41** | **180.3** | 176.4 | 98% | **3,610** |

细层光滑 kernel 单次 303.7 µs；它一次读 u + 3 个 ε 面 + κ̄² + b + inv_diag、写 u
≈ 8 × 5.0M × 4 B = 160 MB，按实测大数组带宽 460 GB/s 的下界是 348 µs ——
**单个 kernel 本身就在天花板上**。所以唯一的空间是"少搬几趟"，即融合。

## 14.3 `_smooth` 的展开

`nu1` / `nu2` / `mg_coarse_sweeps` 都是 **host 侧的 Python 整数**，循环次数编译期已知，
`fori_loop` 的循环边界纯属白给。改成 Python `for` 之后 XLA 跨整条链融合。

**同一个坑 `surface.py` 的 `dilate` 踩过一次**（DESIGN §3.5：静态起点后
28.95 → 4.86 ms，6.0×）。规律：**trip count 是 host 常量时，不要用 `fori_loop`。**

| | ms/triplet | 迭代和 | relres | ΔG_PB |
|---|---|---|---|---|
| 默认（`fori_loop`, min_n=7, sw=50）| 222.3 | 33 | 8.8e-6 | 885.405 |
| 展开 | **198.9** | 33 | 8.8e-6 | 885.405 |
| 展开 + min_n=41 | **176.8** | 29 | 8.9e-6 | 885.406 |

合计 **−20.5%**，ΔG_PB 与迭代数不变。全帧 ΔG_MM/PBSA 242.7 → 约 197 ms。

## 14.4 层级截断消融（`PBParams.mg_min_n`，新参数）

生产网格 161×161×193 → 81×81×97 → 41×41×49 → 21×21×25 → 11×11×13 → 6×6×7。
`min_n` 决定停在哪一档。**终层变大后 `coarse_sweeps` 必须重调**——「252 点上 4 次够用」
（§1 记录的旧结论）不能外推：

| min_n | 终层 | 点数 | sweeps=4 | sweeps=50 |
|---|---|---|---|---|
| 7 | 6×6×7 | 252 | **214.1 ms / 33 迭代** | 222.3 / 33 |
| 21 | 21×21×25 | 11,025 | 260.7 / **43 迭代** ✗ | 211.4 / 31 |
| 41 | 41×41×49 | 82,369 | 381.6 / **69 迭代** ✗✗ | **200.0 / 29** |

终层一欠解就直接反噬迭代数。最优是 **min_n=41 + sweeps=50**。
（上表是展开**前**的数据；展开后同一组合是 176.8。）

**尚未设为默认**：只在 S4 / h=0.5 一个体系上测过，换体系层级结构会变。

## 14.5 试过并证伪的

| 方向 | 结果 |
|---|---|
| XLA CUDA-Graph 开关（`--xla_gpu_enable_command_buffer=…,WHILE`）| **更慢**（240.7 / 233.0 vs 221.8）。不消除循环边界，只加 graph 实例化成本 |
| `--xla_gpu_graph_min_graph_size=1` | 无差别（221.1） |
| 两进程并发测饱和度 | **方法无效**：无 MPS 时同卡两个 context 由驱动时分复用，1.00× 既可能是饱和也可能只是串行化 |
| 挂 OpenMM plugin / 手写 CUDA 来"消掉调度开销" | **前提不成立**：GPU 已 96% 满，没有调度开销可消。手写 kernel 只在"每趟搬更少字节"上能赢，而那是融合的事 |

---

# 15. 误差定位：`δΔG_PB ≈ −δG_L`，以及由此得到的非对称网格

**⚠ 这一节包含一个可能影响当前 headline 数字的发现，尚未定论 —— 见 §15.3 的保留意见。**

## 15.1 恒等式

§0.17 早就测到 `δG_C ≈ δG_R`（h=0.75→0.5：+31.35 vs +31.36，抵消到 **0.01**）。
把它写进差分：

```
ΔG_PB = (G_C − G_R) − G_L
        └── δ 抵消 ──┘   └── 全部离散误差在这 ──┘

  δΔG_PB ≈ −δG_L
```

**C 与 R 共 1655/1835 个原子、共同一张网格，它们的离散误差是同一个东西，逐项抵消；
而孤立配体的 G_L 无可抵消。** 所以 ΔG_PB 的离散误差 = 孤立配体那一次求解的误差。

（对**摆放**扰动这条弱一些但仍成立：origin 挪 h/2 时 δG_C−δG_R = −1.40、−δG_L = −7.00，
配体仍占 83%。）

## 15.2 实测：C−R 早已收敛，G_L 远没有

同一 canonical 帧，swin=0.5，tol=1e-5：

| | h=1.0 | h=0.75 | h=0.5 |
|---|---|---|---|
| 网格 | 97³ | 129³ | 161×161×193 |
| **G_C − G_R** | 254.880 | **265.478** | **265.470** |

`0.75 → 0.5` 只动 **0.008**。（`1.0 → 0.75` 动 10.6 —— h=1.0 太粗，与 §1.2 一致。）

| G_L（180 原子） | 网格 | 节点 | 值 |
|---|---|---|---|
| h=0.5，大盒（complex 包围盒）| 161×161×193 | 5.0 M | −619.935 |
| h=0.5，紧盒（ligand 包围盒）| 161×129×129 | 2.7 M | −610.298 |
| h=0.375，紧盒 | 193×161×193 | 6.0 M | −592.241 |
| h=0.25，紧盒 | 321×225×257 | 18.6 M | **−586.307** |

`0.5 → 0.375 → 0.25` 仍在走 **+18.1、+5.9**（比值 3.07，h 比 1.5 → 阶 ≈ 2.8），
外推 h→0 约 **−583**。**h=0.5 上 G_L 有约 27 kcal/mol 的离散误差。**

> 大盒 vs 紧盒在同一 h 下差 **9.6** —— 这**不是**紧盒更准，而是同一个
> grid-origin 摆放噪声（§15.4 测得 G_L 单独的 origin 峰峰值 7.00）。不要把它算成收益。

## 15.3 组装起来，以及为什么这可能是个正确性问题

```
ΔG_PB = (G_C − G_R)|_{h=0.75}  −  G_L|_{h=0.25, 紧盒}
      = 265.478 − (−586.307) = 851.8        （当前全 h=0.5 共用网格：885.4）
```

对照 Amber pbsa 的 **842.9**：差距从 42.5（5.0%）收到约 **8.9（1.1%）**，
而 §15.4 的摆放噪声本身就有 ±7–8。**那 5% 的方法差异，很可能主要是我们的配体欠解。**

**这不是"优化"，是当前数字可能不对。** ΔG_MM/PBSA 会从 −67.5 移到 −101 量级。

**保留意见（都还没做）**：

- 只在 **S4 的一帧**上测过，一个体系。
- 紧盒改变了 origin 对齐**和** padding（DH 边界距离），两者未拆开。
- `G_L|h=0.25` 本身也未收敛，用的是外推趋势而非收敛值。
- 非对称网格**破坏 DESIGN §2 的"三者共用一张网格"**。§15.1 给了理由（要抵消的只有
  C−R 这一对，L 无可抵消），但这条推理必须在 S1–S3 上独立复核后才能改默认。
- 三个 species 形状不同 → **三次编译**（现在是一次）。可接受，但要记账。

## 15.4 grid-origin 敏感性（新指标）

同一构象、同一 h、同一 shape，**只挪 origin** —— 物理零变化，任何非零都是离散化误差：

| origin 偏移 | 0 | h/4 x | h/2 x | h/4 (111) | h/2 (111) | 峰峰值 |
|---|---|---|---|---|---|---|
| ΔG_PB（swin=0.5）| 885.41 | 883.05 | 881.53 | 880.51 | 877.01 | **8.39** |
| ΔG_PB（swin=0）| 869.21 | 867.09 | 865.75 | 865.21 | 861.99 | **7.22** |
| G_C / G_R / G_L 峰峰值（swin=0.5）| | | | | | 17.53 / 15.55 / 7.00 |

**两条结论：**

1. **摆放噪声 8.39 比整个 h=0.75→0.5 全局加密的效果（5.49）还大。** 不先修界面/源的
   离散化就去加密，是在本底噪声之下抛光。
2. **`swin=0.5` 没有压住它**（8.39 vs swin=0 的 7.22，还略差）。DESIGN §3.5 加这个
   平滑的理由写的是「压帧间 grid 离散噪声」——**这条理由被证伪**。它确实把答案整体
   挪了 16.2，但没降低摆放敏感性。

> 轨迹上这是**帧间噪声**：网格对整条轨迹固定，分子在其中漂移，挪网格 ≡ 挪分子，
> 所以刚体漂移本身就往 ΔG_PB 注入 ±8 kcal/mol，直接进 block-SEM。

## 15.5 由此得到的排序（替代"全局加密"）

> **⚠ 本节排序已被 §15.8 修正**：多中心边界从第 1 项降到第 4 项（边界实测已收敛，
> 它不解锁任何东西），非对称网格升到第 1（实测又快又准）。下表保留原推理供对照。

| # | 方向 | 依据 | 代价 |
|---|---|---|---|
| 1 | **多中心屏蔽库仑边界** | 机器现成（`coulomb_boundary_values`，`atom_block` 已调到 128），加个 `exp(−κr)/(1+κa)` 即可；**~3 ms/帧 = 1.5%**。是 pbsa 差异里点名过的来源之一，零结果也能把剩余归到能量泛函 | 小 |
| 2 | **非对称网格：C/R 粗、L 细** | §15.2–15.3。C−R 在 h=0.75 收敛（0.008），G_L 在 h=0.5 差 ~27 | 中 |
| 3 | **分数体积 ε**（face 被溶质占多少，不是二值）| 直接打摆放噪声（§15.4）和 swin 敏感性（16.2）。**保持 7 点 SPD 算子，求解器不动** | 中 |
| 4 | 曲面解析基准（Kirkwood 球 / 双球）| 现在只有 Born（单球、均匀 ε），测不出曲界面的 voxel 化误差。主张 cut-cell 更准就必须有它 | 小 |
| 5 | NLPB（Newton–Krylov）| `J = −∇·(ε∇·) + κ²cosh(φ)·` 仍 SPD，现有 PCG+MG 只需改对角；S4 的 −4 磷酸肽是正当测试对象。但**是模型变化**，独立立题 | 中 |
| — | ~~全局/局部加密作为首选~~ | 见 §15.4：在噪声底下抛光；且 focusing 会引入三者不同源的细层边界，风险是打坏 C−R 抵消 | — |
| — | ~~高阶电荷分配（spl2/spl4）~~ | **对我们特别地低价值**：G_PB 是两次求解的反应场差，两解共用同一个 `b`，源的离散化误差主要项**已经抵消**。界面不抵消（参考解处处 ε_in），所以「源 + 界面」在这里**不是对称的一对** | — |
| — | ~~Galerkin / 算子相关 MG~~ | ε map **逐帧都变** → `A_H=RA_hP` 必须逐帧重建，只摊到该帧 ~11 次 V-cycle，比例太差。且现在瓶颈是每次 V-cycle 贵，不是不收敛 | — |
| — | ~~IIM~~ | 跳跃条件进 stencil **可能破坏 SPD**，而 MG 在 1:80 下当 solver 本就发散（§1.3）。要做界面就做分数体积 ε，不要做 IIM | — |

## 15.6 冷启动与编译缓存（顺带测的）

| | import+init | 首次编译 | 第二帧 | 总冷启动 |
|---|---|---|---|---|
| 无缓存 | 4.3 s | **36.5 s** | 0.20 s | **42.1 s** |
| 持久缓存命中 | 3.7 s | **5.9 s** | 0.20 s | **10.7 s** |

一行开关，缓存 3.5 MB，**稳态速度不变**：

```python
jaxpbsa.enable_compilation_cache()      # -> ~/.cache/jaxpbsa-xla
```

已在 `crl.py` / `profile_stages.py` / `warm_start.py` 打开。
**`benchmark.py` 刻意不开** —— 它每个配置起独立子进程正是为了消除跨运行状态（§8 陷阱 1），
持久缓存会让首跑与复跑不同。

剩下的 10.7 s 里约 3.7 s 是 Python import，那是 AOT 导出唯一还能拿走的部分 —— 见 §10
为什么不划算。

## 15.7 specialization 是热进程独有的杠杆（不是 AOT 导出）

**要分清两件事**：AOT 导出（`jax.export`）的问题是形状变 → 产物爆炸（§10）；
**运行时 specialization** 没有这个问题，而且是我们唯一的结构性优势。

冷热不对称：

```
Amber   每帧都冷: 起进程 + 读 prmtop + spawn pbsa ×3 + 文件 I/O  -> 5806 ms/帧
我们    一次冷:   12.9 s (编译缓存命中), 之后 0.325 ms... 0.325 s/帧
        -> 12.9 + 0.325N = 5.806N  =>  **第 3 帧回本**
```

**按 species 特化网格的代价（S4，实测）：**

| | 编译 | 每帧 | ΔG_PB |
|---|---|---|---|
| 现状：三者共用 h=0.5（**1 次**编译，靠补齐）| 39.9 s | 197.1 ms | 885.405 |
| 特化：C/R h=0.75 共用盒 + L 紧盒 h=0.25（**3 次**编译）| 67.6 s | 533.7 ms | **851.785** |

| | 节点 | ms/帧 |
|---|---|---|
| C | 2,146,689 | 38.7 |
| R | 2,146,689 | 42.0 |
| **L** | **18,561,825** | **453.0** |

**10,000 帧一条轨迹：现状 33.5 min，特化 90.1 min，编译只占 1.3%。**
多两次编译在热进程里等于免费 —— 冷工具（每帧 spawn 一次 pbsa）结构上付不起。
对照 Amber 串行同样 10,000 帧：**967.7 min**，特化版仍快 **10.7×**，
而且算的是一个 MMPBSA.py 的架构产不出来的数（它三个 species 走同一次调用、同一个 `scale`）。

**代价不在特化机制，在配体那个盒子**：C/R 变粗省 50 ms，L 加密花 388 ms。

## 15.8 padding 超配 2–4×，特化版因此比现状**又快又准**

先记一条**我写完就被自己证伪的推断**：我曾写「多中心边界 → padding 可收 → 配体网格减半」。
**错了。** 单中心 DH 边界在当前 padding 下早已收敛：

| complex 网格 padding | 20 | 23.6 | 30 | 40 |
|---|---|---|---|---|
| ΔG_PB | 885.405 | 885.407 | 885.408 | 885.408 |

20 → 40 Å 只动 **0.003**。边界不是瓶颈，**没有可"解锁"的东西**。
杠杆在反方向：padding 能不能**更小**。

**配体盒实测**（180 原子，净电荷 −4，κ⁻¹=7.86 Å，本体包围盒 ~40×24×24 Å）：

| padding | h=0.5 节点 / G_L | h=0.25 节点 / G_L | 与 pad=20 之差 |
|---|---|---|---|
| 20 | 2.68 M / −610.298 | 18.56 M / −586.307 | — |
| 15 | 1.61 M / −610.290 | 11.16 M / −586.299 | +0.008 |
| **12** | 1.21 M / −610.284 | **6.99 M** / −586.278 | **+0.029** |
| 8 | 0.61 M / −610.209 | **4.01 M** / −586.217 | +0.090 |

**DESIGN §2 的 `padding = max(20 Å, 3κ⁻¹) = 23.6 Å` 被实测超配 2–4×。**
真实需求约 **1.5κ⁻¹ ≈ 12 Å**（0.03 kcal/mol）或 **1κ⁻¹ ≈ 8 Å**（0.09）——
两者都远小于 §15.4 的摆放噪声 8.39。

（顺带：代码里全项目传的都是 `padding=20.0`，**低于**自己写的 23.6 规则。
既然规则本身超配，这个不一致无实际后果，但规则该按实测改。）

### 结果：特化版比现状又快又准

| 方案 | 节点合计 | ms/帧 | ΔG_PB | 距 pbsa 842.9 |
|---|---|---|---|---|
| 现状：三者共用 h=0.5 | 15.0 M | 197.1 | 885.405 | 42.5（5.0%）|
| 特化，配体 padding=20 | 22.9 M | 533.2 | 851.785 | 8.9 |
| 特化，配体 padding=12 | 11.3 M | 220.2 | 851.756 | 8.9 |
| **特化，配体 padding=8** | **8.3 M** | **162.0** | 851.695 | **8.8（1.0%）** |

（特化 = C/R 共用 h=0.75 大盒，L 用紧盒 h=0.25。）

**比现状快 18%，同时把与 Amber 的差距从 5.0% 收到 1.0%。** 这不是取舍，是两边都赢 ——
因为现在是把算力花在**会抵消的 C/R** 上（§15.1），而配体欠解。

**推荐默认 padding=12**（0.029 保守些），padding=8 备用。仍需 S1–S3 复核。

### 这条对 §15.5 排序的修正

多中心屏蔽库仑边界**不再是第 2 项的前置**（边界已收敛，没东西可解锁）。
它的理由回到原来那条、也只剩那条：**它是 pbsa 那 5% 差异里点名过的来源之一，
而且几乎白拿**。但既然特化已把差距收到 1.0%，剩余空间也就 8.8 kcal/mol ——
**它的优先级应当下调到分数体积 ε 之后**。

新排序：**① 非对称网格（§15.8，又快又准） → ② 分数体积 ε（打 8.39 的摆放噪声）
→ ③ 曲面解析基准 → ④ 多中心边界（残余 8.8）→ ⑤ NLPB**。

---

# 16. 在线：同卡调度实测（ONLINE_PLAN S6）

**这一节回答 plan §21.5 约束 4 留下的唯一未知数：MD 和 PBSA 抢同一张卡时，
overhead 公式还成立吗。**

机器：1× RTX 2080 Ti（11 GB，测量时空闲）。MD：S4 溶剂化 **23,847 原子**，PME +
1.0 nm cutoff，HBonds 约束，**2 fs 无 HMR**（与 `run_s4_md.py` 同配置），
LangevinMiddle 298.15 K，每配置 200,000 步。PBSA：`padding=30`、h=0.5、fp32、
`precond="auto"`、`margin_min=12`。每个配置独立子进程（同进程顺序跑会让编译产物和
显存单调累积）。`XLA_PYTHON_CLIENT_PREALLOCATE=false`。

> **体系原子数与 §21.5 记的 20,637 对不上** —— 当前 `data/md/S4_solvated.pdb`
> 是 23,847 原子。以文件为准；§21.5 的 t_step 应当是更早一次溶剂化的测量。

| 查询间隔 | 模拟时间 | wall | **实测 overhead** | 公式 `T_pbsa/(N·t_step)` | reports |
|---|---|---|---|---|---|
| baseline（无 reporter）| — | **54.67 s** | — | — | 0 |
| 每 5000 步 | 10 ps | 71.39 / 71.82 s | **30.6% / 31.4%** | 31.1% | 40 |
| 每 10000 步 | 20 ps | 63.73 / 63.56 s | **16.6% / 16.3%** | 15.9% | 20 |
| 每 25000 步 | 50 ps | 60.03 / 58.57 s | **9.8% / 7.1%** | 6.5% | 8 |

两个数 = `--order openmm` / `--order jax`（先建 Context 还是先预热 JAX）。
baseline：273.4 µs/步、632.1 ns/day。每帧 PBSA 稳定在 **425–445 ms**。

## 16.1 同卡争用没有额外惩罚

实测与公式差 ±1–3 个百分点，而且**不是系统性的**：5000 那档实测 overhead 反而
**低于**公式（PBSA 累计 17.03 s 比 wall 增量 16.72 s 还大），25000 那档两个 order
自己就差 2.7 个点。这个量级就是 MD 的跑间方差。

**结论：MD 与 PBSA 是干净串行的，没有显存争抢或上下文切换的额外代价。**
plan §21 的双 GPU async worker 到此为止**没有证据支持** —— 单卡上换调度方式不改变
GPU 总工作量，能改变的只有「换第二张卡」。

## 16.2 按模拟时间，与 §21.5 的推算表重合

按**步数**看，practical 档位比 §21.5 右移了一档；按**模拟时间**看基本重合：

| 查询间隔 | §21.5 推算（4 fs + HMR，222 ms/帧）| 本次实测（2 fs 无 HMR，438 ms/帧）|
|---|---|---|
| 每 20 ps | 18% | **16%** |
| 每 40–50 ps | 9% | **6.4–8%** |
| 每 100 ps | 3.6% | **3.2%**（外推）|

**两个 2× 抵消了**：在线默认 `padding=30` 的网格是 225×193×225 = 9.77 M 节点，
比 §3 那个 `padding=20` 的 161×161×193 = 5.0 M 大 **1.95×**（所以每帧 438 而不是
222 ms）；而本次 MD 每模拟 ps 慢 **2.2×**（无 HMR、2 fs）。

所以 §21.5 的结论**实测站得住**：每 20–40 ps 查一次落在 practical 区间，
每 100 ps 可以 always-on。交叉验证：632 ns/day × 2（4 fs + HMR）≈ 1264，
与 §21.5 记的 1397 ns/day 同量级。

## 16.3 这一节**没有**证明的

- **双 GPU 没测** —— 本机只有一张卡。坐标 22 KB / ~1 µs 的判断仍是推算。
- **`--order` 的两列不可读**：第一次跑要付冷编译（43.1 s vs 缓存命中 8.5 s），
  先后顺序本身也影响 GPU 时钟。两列差异应当归给噪声，不是顺序效应。
- ~~**被 flag 的帧没有落盘**~~ —— 已修。当时配置没给 `out_csv`，而 `PBSAReporter`
  在 `on_violation="flag"` 且无 CSV 时会把违规字符串直接丢掉，所以那 68 帧里有没有
  `margin < 12` 或未收敛无从得知。现在 flag 会计数、首次打印、`close()` 汇总
  （见 §16.7 的实跑：6 帧无 flag）。
- **只有一条 5 ps 起步的轨迹片段**，不是长程行为。

## 16.4 顺带核实：多 reporter 的 `enforcePeriodicBox`

生产跑必然同时挂 `DCDReporter`（它要 wrap 后的坐标），而我们要**未 wrap** 的。
OpenMM 8.5.2 的 `Simulation._simulate` 把同一步触发的 reporter 按 wrap 需求
**分成 wrapped / unwrapped 两组，各取一次 `getState`**，所以两者不冲突 ——
不会出现「别人要 wrap，我们被顺带 wrap 了」的静默错数。6 元组的旧格式也仍被
转换（`report[5] → periodic`）。**更老的 OpenMM 没核过，降版本前要重查这一段。**

## 16.5 host 往返的实测值 —— plan §21.5 的 `1e-5` 是纸面数

plan §21.5 约束 1 用「溶质坐标 22 KB / PCIe 25 GB/s ≈ 1 µs」推出「拷贝占 1e-5」。
实测（2080 Ti，S4 溶剂化 23,847 原子，30 次取中位）：

| | |
|---|---|
| `getState(getPositions, enforcePeriodicBox=False)` + `asNumpy` | **0.95 ms**（0.86–2.43）|
| 切片到溶质 + 质心归位（1835 原子，host numpy）| **0.081 ms** |
| 合计 / 一帧 PBSA 431 ms | **≈ 0.24%** |

**差 100 倍，因为真实代价不是那 22 KB。** `getState` 搬的是**全体系 fp64**
（559 KB）外加它自己的同步开销；溶质 fp32 的 22 KB 只是其中一小块。

结论不变（都可忽略），但这个数决定了一件事：**OpenMM-native 插件（PJRT / `JaxForce`
路线）能省的性能上限就是 0.24%。** 编译也不是它能省的 —— 编译是**每进程一次**
（缓存命中 9.7 s / 冷 43 s），不是每帧；40 个 report 稳定在 425–445 ms 就是没有
重编译的直接证据，而 PJRT 那边同样要在 Context 建立时 load executable。

**所以插件的立项理由只能写成 capability，不能写成性能**：唯一不可替代的是
「PB 作为 force group 31 的可查询 CV 真正进入 MD」（plan §21.5 的形态）。
按 0.24% 去论证它，benchmark 一跑就打脸。

## 16.6 reporter 不扰动轨迹，但 OpenMM CUDA 本来就不可逐位复现

问题：挂 `DCDReporter` / `PBSAReporter` 会不会改变 MD 轨迹本身。
reporter 只读状态、不回写 Context（`enforcePeriodicBox` 只作用在导出的那份拷贝上），
也不碰积分器的随机数流 —— 但这是**推理**，实测如下（同一起点，
`setRandomNumberSeed(12345)`，A=无 reporter，B=有，C=重复 A 作对照）：

| | A vs C（对照：同配置跑两遍）| A vs B（无 vs 有 reporter）|
|---|---|---|
| 20 步 | 1.907e-05 Å | 2.146e-05 Å |
| 2000 步 | 1.080e+01 Å | 1.049e+01 Å |

**挂 reporter 的影响与「同配置跑两遍」同量级** —— 即 reporter 无影响，而 OpenMM
CUDA 平台**自身**就不是逐位可复现的（20 步就有 2e-5 Å）。没有对照组的话，2000 步
那 10 Å 会被误读成「reporter 扰动了动力学」，其实那只是 1e-5 被混沌放大到 Å 量级
（换随机种子给出 1.245e+01，同量级）。

> **对「在线 vs 离线对比」的直接后果**（ONLINE_PLAN §7 的待办）：单条 MD 轨迹
> 不可复现，所以对比**必须在同一批已保存的帧上做**，不能「重跑一遍 MD 再比」——
> 否则比的是两条不同的轨迹，散点里混着混沌发散，和归位方式的 ±8 分不开。

## 16.7 生产配置实跑：DCD 比 PBSA 还贵

`DCDReporter`(1 ps) + `PBSAReporter`(50 ps，间隔取整数倍) 并存，150k 步（300 ps）：

```
analyzer init 9.7s | grid (193,225,225) h=0.5
150k steps wall=49.59s  t_step=330.6us  ns_day=522.7
[PBSAReporter] 6 帧, PBSA 累计 2.58 s, 均值 430.6 ms/帧, 无 flag
```

| step | time_ps | ΔG_PB | ΔG_SA | ΔG_MMPBSA | iters C/R/L | margin_A |
|---|---|---|---|---|---|---|
| 5575000 | 11150 | 795.79 | −5.81 | −62.98 | 12/12/10 | 22.70 |
| 5600000 | 11200 | 787.72 | −5.41 | −77.99 | 12/11/10 | 22.28 |
| 5625000 | 11250 | 789.12 | −5.45 | −76.64 | 12/12/10 | 23.38 |
| 5650000 | 11300 | 832.56 | −5.81 | −67.08 | 13/12/10 | 21.66 |
| 5675000 | 11350 | 860.37 | −5.48 | −59.59 | 12/12/10 | 22.20 |
| 5700000 | 11400 | 799.05 | −5.75 | −71.39 | 11/11/10 | 21.02 |

- **step 是绝对步数**（从 `S4_prod_final.xml` 恢复的 5.575 M 步 / 11.15 ns）。
  `describeNextReport` 用 `currentStep % interval`，所以取帧落在绝对步数的整数倍上，
  **断点续跑后仍对齐**。
- **ΔG_PB 的抖动远大于 ΔG_MMPBSA**：250 ps 内 ΔG_PB 峰峰 72.6，ΔG_MMPBSA 只有 18.4
  （ΔE_MM 与 ΔG_PB 反相关吃掉了大部分）。72.6 里有多少是 §15.4 那 8.39 的摆放噪声、
  多少是真构象，**6 帧看不出来**。
- **DCD 比 PBSA 贵**：按 baseline 273.4 µs/步反推纯 MD 应 41.0 s，实测 49.59 s，
  多出 8.6 s 里 PBSA 只占 2.58 s，剩下 ~6 s 是 DCD@1 ps 写 300 帧 × 23,847 原子
  （≈86 MB）。**每 50 ps 的 PBSA 监控比它搭车的轨迹写盘还便宜。**（拆分有 ±2 s
  跑间方差，别当精确值。）省法与 PBSA 无关：不写、只写溶质
  （`mdtraj.reporters.DCDReporter(..., atomSubset=solute_idx)`，1835/23847 = 13× 更少，
  顺带省掉 `strip_unwrap.py`）、或降频。

## 16.8 网格定尺：白多了两档 dime，−26.9% 时间且 ΔG 不变

`padding` 这个旋钮是**量化**的 —— 真正决定盒子的是 APBS dime 集合。从 10 ns 干轨迹
反推（质心归位后每轴 `max|x − COM|`，与在线归位同约定）：

```
10000 帧, 每轴半长: 帧最大 [24.36, 25.82, 26.86] Å,  轨迹内涨落 [7.13, 9.41, 8.78]
需要的半长 = 帧最大 + reach 5.7 + margin_min 12 = [42.06, 43.52, 44.56]
```

同一帧、同一台卡实测两个网格：

| | 网格 | 节点 | ms/帧 | 本帧 margin | ΔG_PB | ΔG_MMPBSA |
|---|---|---|---|---|---|---|
| 当前 `padding=30` | (193,225,225) | 9.77 M | **426.4** | 24.0 Å | 782.57 | −66.94 |
| 恰好够用 `padding=25.35` | (193,193,193) | 7.19 M | **311.9** | 18.1 Å | 782.57 | −66.94 |

**−26.9% 时间 / −26.4% 节点，几乎线性**（PB 访存受限，符合 §14 的归因）；
**ΔG_PB 与 ΔG_MMPBSA 到小数点后两位完全相同** —— 边界早已收敛（§15.8：
padding 20→40 只动 0.003），多出的两档是纯浪费。193³ 在全轨迹上最小 margin 仍有
**15.4 Å**。

> 两个网格的 origin 差 8 Å = 16h，是 h 的整数倍，所以亚格点相位相同 ——
> 这正是 §15.4 那 8.39 的摆放噪声**没有**混进这个对比的原因。不是巧合。

代入 §16 的 overhead（baseline 273.4 µs/步）：

| 查询间隔 | 现在 426 ms | 换 193³ 312 ms | §22 档位 |
|---|---|---|---|
| 每 10 ps | 31% | **23%** | 离线 → 降低频率 |
| 每 20 ps | 16% | **11.4%** | 降低频率 → **practical** |
| 每 50 ps | 6.4% | **4.6%** | practical → **always-on (<5%)** |

**只是初始化时把 dime 选对，每个档位整体上移一格** —— 不改算法、不动求解器、不加卡。
另：质心归位后三轴需求几乎各向同性（42.1/43.5/44.6），所以在线场景下矩形网格那
−30.4%（§2）的收益大半消失，当前 (193,225,225) 的不对称是**参考帧包围盒的偶然**，
不是体系的性质。

## 16.9 试跑要多长：单帧不够，1 ns 够 —— 与 SA 的 `k_neighbors` 同一个判决

按轨迹前缀定尺，再放到全 10 ns 上检验：

| 用多少帧定尺 | 该前缀的所需半长 | 欠全轨迹 | 定出的网格 | 全轨迹最小 margin |
|---|---|---|---|---|
| 1 帧 | 23.08 Å | **3.79** | (161,193,161) 5.00 M | **+7.44**（< 阈值 12）|
| 100 帧（0.1 ns）| 25.40 | 1.46 | (193,193,161) 6.00 M | **+7.44**（< 阈值）|
| 1000 帧（1 ns）| 25.40 | 1.46 | (193,193,193) 7.19 M | +15.44 OK |
| 10000 帧（10 ns）| 26.86 | 0.00 | (193,193,193) 7.19 M | +15.44 OK |

**需求是单调增的**（5 ns 仍欠 1.04），因为它是 max 统计量 —— 跑得越久越可能撞上更大的
涨落，永远不会真正收敛。这与 §11.2 里 SA 的 `k_neighbors` 是**同一个问题**
（单帧探不出系综上界），所以应当抄同一个判决：**不自动探测，显式参数 + 每帧硬验**。

**失败模式是安全的**：按单帧定尺跑满 10 ns，最小 margin 是 +7.44 —— 低于物理阈值 12，
但**仍大于 0**，没有原子被悄悄丢掉，只是边界贴近。`margin_A`/`margin_min` 会把它
flag 出来。**自动定尺是优化，守卫才是正确性。**

**风险不对称**：多一档 dime 只损失 27–35% 吞吐，少一档赔上整条轨迹。默认必须偏保守。

换体系时泛化的是**流程不是数字**：`reach` 从该体系半径现算（自动对）、
`margin_min ≈ 1.5κ⁻¹` 应从离子强度现算（**现在钉死 12 是 bug** —— 0.05 M 下该是 ~20）、
`fluctuation_allowance` **不泛化**，必须显式给或从试跑量。以上只有 S4 一个体系的证据。

---

# 17. 非对称网格设为默认 + 离线质心归位（2026-09-23）

新入口 `jaxpbsa.pb.TripletSolver`：离线脚本（`crl.py` / `validate_mmpbsa.py`）与在线
`OnlineMMPBSA` 走**同一个**类。两件事一起做：

- **质心归位**：C/R 按复合物质心、L 按**配体自己的**质心，各自放回自己网格的中心
  （ONLINE_PLAN §7 的待办，离线侧此前是「整轨迹共网格、坐标不动」）。
- **默认网格**：C/R 共用 h=0.75（padding 20），配体单独 h=0.25 紧盒（离线 padding 8，
  在线 14，见 §17.4）。`h_lig=None` 退回旧的三者共用一张网格。

环境：2080 Ti。**注意 `openmm_dev` 现在实际加载的是 jax/jaxlib 0.10.2**（pip 记着 0.11.1，
0.11.1 的 CUDA plugin 因版本不符被跳过，但 GPU 仍可用）。旧路径在 canonical 帧上
逐位复现 885.405 / 11-12-10 次迭代 / 197.0 ms，所以本节与之前的数可比。

## 17.1 §15.2 的「C−R 在 h=0.75 已收敛（0.008）」是一次摆放的巧合

同一 canonical 构象、固定网格，刚体平移 7 种亚格点相位（0、h/4·x、h/2·x、h/2·y、
h/4·(111)、h/2·(111)、(0.33,0.71,0.12)·h）：

| | 均值 | sd | 峰峰 |
|---|---|---|---|
| G_C − G_R，h=1.0 | 237.42 | 12.37 | 42.5 |
| G_C − G_R，h=0.75 | **264.98** | **6.04** | **18.0** |
| G_C − G_R，h=0.5 | **264.19** | **3.24** | **9.9** |
| G_L，h=0.5（紧盒 pad 8）| −617.18 | 3.42 | 11.1 |
| G_L，h=0.375 | −591.18 | 1.32 | 3.5 |
| G_L，h=0.25 | **−586.24** | **0.69** | 2.2 |

- **按相位平均，C−R 在 0.75 与 0.5 之间只差 0.8** —— 结论「C−R 无系统偏差」仍成立，
  但 §15.2 那个 0.008 是单一摆放下的巧合。C−R 的**逐帧摆放噪声** 0.75 下是 0.5 的 2 倍。
- **G_L 的偏差是真的，且按相位平均后更确定**：h=0.5 → 0.25 差 **31**（sd 3.4 / 0.7）。
- 所以 §15.3 的推断**方向对、机制要改写**：非对称网格消掉的是配体的系统偏差（~31），
  代价是 C/R 在 0.75 下逐帧噪声 sd 3.2 → 6.0。

按相位平均的组装：共用 h=0.5 ≈ 264.2 + 617.2 = **881**；C/R 0.75 + L 0.25 ≈ **851**；
C/R 0.5 + L 0.25 ≈ **850**；Amber pbsa（canonical）842.9。

## 17.2 20 帧轨迹：两种 C/R 间距的取舍

10 ns 轨迹每 500 帧取一帧，共 20 帧；网格按归位后的 20 帧共同范围建：

| | 网格 | ms/帧 | ⟨ΔG_PB⟩ | 与共用 h=0.5 的逐帧差 sd |
|---|---|---|---|---|
| 旧：共用 h=0.5，**不归位** | (193,193,193) | 284.9 | 816.06 | — |
| 共用 h=0.5 + 质心归位 | (193,193,193) | 284.6 | 816.11 | — |
| C/R h=0.5 + L h=0.25 | (193,193,193) + (225,161,193) | 329.4 | 784.85 | 3.17（= G_L 在 0.5 下的噪声）|
| **C/R h=0.75 + L h=0.25（默认）** | (129,129,129) + (225,161,193) | **208.8–225.2** | **778.46** | 8.92 |

> **⚠ 下面这段理由已被 §18.5 推翻**：质心归位把 C/R 在 0.75 下的摆放误差冻成了逐帧一致的
> 常数偏差（~+10），不会随帧平均掉。

**默认取 h=0.75**：帧是隔很久取一个的（在线每 20–50 ps），逐帧摆放噪声与构象涨落
独立、同样随帧数平均掉。实测帧间 sd **31.4**（C/R 0.75）vs **31.0**（C/R 0.5）——
方差只多 2.6%，而同样算力多拿 46% 的帧。

平均 20 帧，归位与否 ⟨ΔG_PB⟩ 只差 0.05，**但逐帧差到 ±9**（例：帧 1500 823.3 → 832.1，
帧 6000 818.9 → 810.3）—— 这就是 §15.4 的刚体漂移摆放噪声，平均掉了但逐帧存在。
在线 vs 离线逐帧比较之前**必须**两边同一套归位（现在是同一个类，自动满足）。

## 17.3 外部对拍：与 MMPBSA.py 的差距 5.0% → 0.26%

> **⚠ §18.4：1YCR 上同一默认差 −5.9%，这个 0.26% 不是一般性结论。** C/R 0.75 带 ~+10
> 的冻结偏差（§18.5），扣掉后 S4 约 +0.55%、1YCR 约 −2.3%；剩余差异的来源未测。

`scripts/validate_mmpbsa.py --frames 20 --stride 500`（pbsa 仍 h=0.5，参数逐项锁死，同 §12）：

| 项 | MMPBSA.py | jaxpbsa | Δ | rel |
|---|---|---|---|---|
| ΔE_coul | −826.830 | −826.859 | −0.029 | 3.5e-05 |
| ΔE_LJ | −44.655 | −44.657 | −0.002 | 4.4e-05 |
| **ΔG_PB** | **780.518** | **778.456** | **−2.062** | **2.6e-03** |
| ΔG_SA | −5.785 | −5.784 | +0.002 | 2.6e-04 |
| **ΔG_MM/PBSA** | **−96.753** | **−98.844** | −2.091 | 2.2e-02 |

§12.2 判定「那 5% 是方法差异、不会收敛掉」—— **撤回**。两边各自「收敛」的判据都是
单一摆放下的加密，而我们这边欠解的是配体、且被 C/R 的抵消掩盖了。配体一加密，
差距只剩 2 kcal/mol，已在 C/R 摆放噪声（sd 6）的 1/3 以内。

## 17.4 在线的配体紧盒要多大

在线只有参考帧。磷酸肽很软，按配体质心的每轴半长：

| | x | y | z |
|---|---|---|---|
| 制备态 | 16.01 | 7.52 | 12.27 |
| 前 1 ns 最大 | 17.76 | 11.33 | 14.42 |
| 10 ns 最大 | 17.81 | **12.72** | 16.14 |

y 轴涨 5.2 Å —— 离线的 padding 8 在线会越界。在线默认 `padding_lig = 8 + 6 = 14`。
与 §16.9 同一个判决：max 统计量单调增，**换配体必须重量**。守卫分两路：
`margin_A`（C/R 网格，reporter 按 `margin_min` 判）与 `margin_lig_A`（配体紧盒，只判 < 0 ——
它按设计贴近边界，padding 8 时只剩 ~2 Å，`margin_min=12` 套上去会逐帧误报）。

## 17.5 没有做的

- 只有 S4 一个体系（§15.3 的「S1–S3 复核」仍未做）。
- 在线默认改了网格（C/R 0.75 / padding 30 → (129,129,129) 级别），§16 的 overhead 表
  **没有重测**；按 §17.2 的时间比例，每帧应从 ~430 降到 ~250 ms 量级，未实测。
- `benchmark.py` / `profile_stages.py` / `warm_start.py` 是单 species 性能脚本，未改。

---

# 18. 第二个体系 1YCR，以及质心归位把 C/R 的摆放误差冻成了常数偏差（2026-09-23）

**⚠ 本节推翻 §17.2 的「默认 C/R h=0.75」理由，以及 §17.3 / README 的「与 Amber 差 0.26%」
作为一般性结论。代码默认值尚未改动 —— 两个修法待定（§18.6）。**

## 18.1 体系

1YCR：MDM2 25–109（受体，1458 原子）+ p53 17–29 `ETFSDLWKLLPEN`（配体，230 原子），
两链都加 ACE/NME 帽（都是片段，自由端电荷在弱静电体系里不是小扰动）。净电荷 +5 / −2
（S4 是 +4 / −4），结合以疏水为主：F19 / W23 / L26 插进 MDM2 口袋。
`scripts/prep_1ycr.py`，产物同 S4 格式，**两次独立运行 sha256 逐位相同**
（cif `1944cfd6…`，system `a05faefb…`）。真空最小化后重原子对晶体 RMSD 0.845 Å。

MD 同 S4 协议（`run_s4_md.py --name 1YCR`，10 ns，1 ps/帧，**RTX 5080 上 749 ns/day**）。
p53 全程结合：MDM2 Cα RMSD 均值 0.98 Å；p53 Cα（MDM2 参考系）均值 1.72、最大 3.60 Å；
三个 hotspot 侧链到 MDM2 的最小距离均值 3.37 / 2.93 / 3.57 Å，**> 4.5 Å 的帧 0%**；
界面接触首 1 ns 190、末 1 ns 191。

## 18.2 摆放扫描（canonical 帧，7 种相位，`scripts/grid_check.py --name 1YCR`，5080）

| | 均值 | sd | 峰峰 |
|---|---|---|---|
| C−R，h=0.75 | 117.78 | 3.71 | 9.47 |
| C−R，h=0.5 | 120.69 | 2.01 | 5.39 |
| G_L，h=0.5 | −305.71 | 1.53 | 5.18 |
| G_L，h=0.375 | −284.19 | 1.27 | 3.86 |
| G_L，h=0.25 | −281.15 | 0.36 | 1.10 |

**配体偏差照样大：h=0.5 → 0.25 差 24.6**（S4 是 31）。我事前预言「p53 只带 −2，偏差会小得多」
——**错了**：网格误差来自每个原子部分电荷的自能，不是净电荷。「ΔG_PB 的离散误差在配体」
在两个体系上都成立。

## 18.3 20 帧（每 500 帧取一，5080）

| | 网格 | ms/帧 | ⟨ΔG_PB⟩ | 帧间 sd |
|---|---|---|---|---|
| 默认非对称 | C/R (129,129,129) + L (193,193,193) | **89.1** | 275.31 | 23.44 |
| 共用 h=0.5 | (193,193,193) | 124.6 | 313.04 | 22.30 |

非对称 − 共用：−37.73 ± 6.10（S4 也是 −37.7）。

## 18.4 对 MMPBSA.py：S4 那个 0.26% 是运气

| | MMPBSA.py | jaxpbsa（非对称默认）| Δ | rel |
|---|---|---|---|---|
| ΔE_coul | −257.416 | −257.429 | −0.013 | 5.0e-05 |
| ΔE_LJ | −74.627 | −74.627 | +0.000 | 1.7e-06 |
| **ΔG_PB** | **292.656** | **275.311** | **−17.345** | **5.9e-02** |
| ΔG_SA | −7.488 | −7.481 | +0.007 | 9.6e-04 |
| ΔG_MM/PBSA | −46.876 | −64.226 | −17.350 | — |

ACE/NME 帽走 `build_parms` 无问题，ΔE_MM 仍逐位级。**pbsa 自己已收敛**：`--pbsa-h 0.25`
给 292.827（0.5 → 0.25 只动 0.06%，与 S4 相同）—— 差距不在 pbsa 网格。

## 18.5 真凶：质心归位 + h=0.75 = 不会被平均掉的常数偏差

同 5 帧（0/2000/4000/6000/8000），只改**我们的**网格（`data/md/grid_sens_1ycr.py`）：

| | C/R 网格 | L 网格 | ⟨C−R⟩ | ⟨G_L⟩ | ⟨ΔG_PB⟩ | 对 A |
|---|---|---|---|---|---|---|
| A 默认 h 0.75/0.25 pad 20/8 | (129,129,129) | (193,193,193) | −71.542 | −338.856 | 267.314 | — |
| B padding 40/20 | (193,193,161) | (321,321,321) | −71.542 | −338.860 | 267.318 | **+0.004** |
| C **C/R h=0.5** | (193,193,161) | (193,193,193) | −60.936 | −338.856 | 277.920 | **+10.606（sd 1.70）** |
| D L h=0.2 | (129,129,129) | (225,225,257) | −71.542 | −340.062 | 268.520 | +1.206（sd 0.31）|

- **padding 在 1YCR 上也无关**（B：0.004）。B 连摆放噪声都没有：网格以质心为中心、
  维数为奇数，加大 padding 只在外圈加节点，相位不变 —— 这让 B 成了 padding 的干净隔离。
- **配体已收敛**（D：1.2）。
- **C/R 在 h=0.75 有 ~10.6 的系统偏差，且逐帧一致（sd 1.70）**。

机制：`recenter_com` 每帧把质心放在**同一个网格节点**上 → 亚格点相位**全程冻结**。
这正是它的设计目的（在线 ΔG(t) 不抖），但副作用是摆放误差也被冻住，**从逐帧随机噪声
变成一个固定偏移，帧再多也平均不掉**。S4 的扫描里早就有：h=0.75 在 shift (0,0,0)
——正是质心落在节点上那个相位 —— 给 **275.04**，而相位平均 264.98，**+10.1**；
h=0.5 同一相位只偏 +0.5。S4 的 20 帧里 C/R 0.5 比 0.75 平均高 6.4（SE ~1.7），
当时被我当成噪声读了。

**所以 §17.2「帧隔得远，摆放噪声与构象独立、随帧平均掉，方差只多 2.6%」的前提不成立**
—— 那是对随机噪声的论证，而质心归位已经把它变成了偏差。

按 C 的偏移修正后，与 Amber：1YCR −17.5 → 约 **−7（−2.3%）**，S4 约 **+0.55%**。
剩下的几个百分点看起来是表面定义（`swin`，S4 上整体挪 16.2，§15.4），未测。

## 18.6 待定：两个修法（代码未改）

| | 偏差 | 逐帧噪声 | 代价 |
|---|---|---|---|
| ① C/R 回到 h=0.5 | ~0.5 | sd ~2 | 慢：S4 2080 Ti 225 → 329 ms/帧 |
| ② 保持 0.75，每帧在质心归位上叠加随机亚格点偏移 ∈ [0,h)³（按帧号取种子，可复现）| 相位平均后 ~1–3 | sd ~4–6 | 快；在线 ΔG(t) 多 ~5 的逐帧抖动 |

② 需要验证：同 20 帧上 0.75 + 随机相位的均值与 0.5 差在 ~2 以内，S4 与 1YCR 都要。

## 18.7 顺带记下的（在线网格尺寸，已停测）

- §16 overhead 在新默认（`padding_lig=14`）下重测到一半后停止。已完成的：baseline 270.1 µs/步
  （639.8 ns/day）；每 5000 步 28.6% / 28.8%；每 10000 步 14.4% / 16.0%；PBSA **363–372 ms/帧**
  （§16 旧默认 ~430），40+40+20+20 帧均无 flag。每 25000 步未测。
- **慢在配体盒**：yayoi38（5080）实测，在线 padding_lig=14 的 L 网格 (257,193,225) 11.16 M
  节点耗 158.2 / 204.5 ms（77%）；离线 padding 8 的 (225,129,193) 5.60 M 只要 48.4 ms。
  C/R (161,129,161) 两者都是 45 ms。同机 import 16.8 s（本机 133–356 s，NFS 冷读 7.5 MB/s）；
  本机 warmup 冷/热 93 / 13 s 而每帧 363.5 / 367.8 ms 不变 —— **NFS 只影响启动，不影响每帧**。
- 配体盒按轴涨落不均（x +1.8、y +5.2），统一 padding 再被 dime 取整放大。S4 10 ns 上：

  | 定尺 | L 网格 | 节点 | 全程最小 margin_lig |
  |---|---|---|---|
  | 单帧 + padding 8 | (225,129,161) | 4.67 M | **−2.42（丢原子）** |
  | **单帧 + padding 10** | (225,161,193) | 6.99 M | +1.58 |
  | 单帧 + padding 14（现默认）| (257,193,225) | 11.16 M | +5.58 |
  | 0.5 / 1 ns 试跑 + padding 8 | (225,161,193) | 6.99 M | +1.58 |

  单帧 + 10 与 1 ns 试跑 + 8 给出同一网格，也正是 §17.3 对拍用的那张。**默认未改。**

## 18.8 ② 证伪：随机相位不改均值 —— C/R 在 h=0.75 的偏差是真离散误差，不是相位冻结（2026-09-24）

`scripts/phase_jitter_check.py`（2080 Ti，同 §17.2 / §18.3 的 20 帧，配体一律 h=0.25）。
`jitter75` = 质心归位后 C/R 再平移 U[0,h)³（种子 = 帧号，`TripletSolver(..., shift=)`）。
`frozen75` 逐位复现 778.456 / 275.311。

| | S4 ⟨ΔG_PB⟩ | 对 frozen50 | 对 MMPBSA.py | 1YCR ⟨ΔG_PB⟩ | 对 frozen50 | 对 MMPBSA.py |
|---|---|---|---|---|---|---|
| frozen75（当前默认）| 778.456 | −6.39 ± 1.78 | −0.26% | 275.310 | −8.94 ± 1.53 | −5.93% |
| **jitter75** | 778.071 | **−6.78 ± 2.08** | −0.31% | 273.115 | **−11.14 ± 1.82** | −6.68% |
| frozen50 | 784.848 | — | +0.55% | 284.254 | — | −2.87% |

（± 为配对差的 SE；ms/帧 0.75 ≈ 205，0.5 ≈ 327。）

- **两个体系都 FAIL**（判据 < 2）。相位打散后均值不动，逐帧 sd 反而 +1.2 —— 偏差在相位平均后仍在。
- 所以 §18.5 的**机制写错了**：~−7 到 −11 是 C−R 在 h=0.75 的**相位平均**离散误差，质心归位只是让它
  逐帧一致，不是它的来源。§17.1 / §18.2 的单构象 7 相位扫描（0.8 / 2.9）样本太少（SE ~2），
  且 canonical 构象不代表轨迹 —— 20 帧 × 随机相位才是相位平均。
- 「C 与 R 的离散误差完全抵消」在 h=0.75 下**不成立**；在 h=0.5 下是否成立**未测**（需 C/R h=0.375 对照）。
- ② 作废，剩 ①（C/R h=0.5，慢 ~60%：本表 205 → 327 ms/帧）。1YCR 在 ① 下对 MMPBSA.py 仍差 −2.9%，剩余来源未测。
- **默认已改（2026-09-24）**：C/R h=0.5，配体 0.25（`TripletSolver` / `OnlineMMPBSA` / `crl.py` / `validate_mmpbsa.py`）。

## 18.9 方案 A：分数面 ε（`PBParams(surface="fraction")`，默认仍 "binary"）+ MG 粗化修正（2026-09-24）

**分数面 ε**（`surface.ses_level` + `_fraction_faces`）：SAS 距离 d 用 scatter-min 精确算，
SES 水平集 G(p) = max_{d(c)≥0} (R_p + d(c) − |p−c|)（G≥0 溶剂），面上按 G 线性插值求溶质占边长
比例 θ，1/ε_f = θ/ε_in + (1−θ)/ε_out（pbsa smoothopt=1 同类），swin 不用。算子仍 7 点 SPD。
顺带发现二值 erode(dilate) 用格点球采样探针：h=0.75 沿轴只够到 0.75 Å（R_p=1.4），方向性偏差。

Born（q=+1, a=2, ε 1/80, κ=0，8 个随机相位，`data/md/born_surface.py`）：

| h | binary 误差 / 相位 sd | fraction 误差 / 相位 sd |
|---|---|---|
| 1.0 | +11.26% / 3.80 | **+3.23% / 0.32** |
| 0.75 | +5.73% / 1.13 | **+1.58% / 0.27** |
| 0.5 | +3.15% / 0.44 | **+0.56% / 0.07** |

1YCR 20 帧（`phase_jitter_check.py --surface`，2080 Ti，MG 修正后）：

| | binary | fraction |
|---|---|---|
| jitter75 − frozen50 | −11.14 ± 1.82 | **+5.09 ± 0.40** |
| 逐帧 sd（摆放噪声）| 8.14 | **1.77** |
| frozen50 对 MMPBSA.py | −2.87% | +3.20% |
| ms/帧 0.75 / 0.5 | 179 / 284 | **231** / 400 |

- fraction 仍 FAIL（判据 < 2），但偏差减半、噪声 1/4.6；**fraction 0.75（231 ms）已比现默认 binary 0.5（284）快**。
- 表面定义变了，整体上移 ~18（对 Amber −2.9% → +3.2%，用户：~3% 不管）。
- 慢的原因不是建图（+3–11 ms/次），是**迭代数**：硬 1:78.5 跳变（无 swin）下 MG 预处理变差。

**MG 粗化修正（已生效，对 binary 默认也是白给）**：`_coarsen_faces` 横向由注入改为 [1,2,1]/4
并联平均。1YCR canonical 迭代 C/R/L：binary 0.5 11/11/9 → 8/9/8；fraction 0.5 15/15/16 → 11/11/11。
binary 20 帧 ΔG 逐位不变（275.310 / 273.115 / 284.254），**默认每帧 326 → 284 ms（−13%）**。

**下一步（未跑）**：`data/md/surface_variants_1ycr.py`——5 帧 × 随机相位只算 C−R 的 0.75−0.5 差，
三个候选来源：base / `arith`（面上算术混合）/ `ion_level`（离子排除用 G ≥ r_ion 代替二值膨胀）。
